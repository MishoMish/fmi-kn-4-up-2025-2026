# **ТЕОРИЯ - ДВУМЕРНИ МАСИВИ (МАТРИЦИ)**

---

# _КАКВО Е ДВУМЕРЕН МАСИВ (МАТРИЦА)?_

## Основни понятия

Двумерният масив е **масив от масиви** - структура от данни, която съхранява елементи в мрежова форма с редове и колони. Визуално може да се представи като таблица или математическа матрица.

### Защо двумерни масиви?

1. ✅ **Организация на данни** - идеални за табличен формат (електронни таблици, шахматни дъски)
2. ✅ **Математически операции** - представяне на матрици от линейна алгебра
3. ✅ **Игри и графики** - карти, мрежи, изображения (пиксели)
4. ✅ **Ефективен достъп** - директен достъп до всеки елемент чрез два индекса O(1)

---

## 1) Деклариране и инициализация

### Основен синтаксис:

```cpp
<тип> <име>[<редове>][<колони>];
```

### Различни начини за деклариране:

#### 1. Деклариране без инициализация:

```cpp
int matrix[3][4];  // 3 реда, 4 колони - съдържа случайни стойности (боклук)
```

#### 2. Инициализация с нули:

```cpp
int matrix[3][4] = {0};  // Всички елементи са 0
```

#### 3. Инициализация със стойности (едноредово):

```cpp
int matrix[2][3] = {1, 2, 3, 4, 5, 6};
// Автоматично се подрежда:
// 1 2 3
// 4 5 6
```

#### 4. Инициализация със стойности (многоредово - препоръчително):

```cpp
int matrix[3][3] = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};
// По-четливо и ясно показва структурата
```

#### 5. Частична инициализация:

```cpp
int matrix[2][3] = {
    {1, 2},     // Третият елемент е 0
    {4}         // Втори и трети са 0
};
// Резултат:
// 1 2 0
// 4 0 0
```

#### 6. Автоматично определяне на първото измерение:

```cpp
int matrix[][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
// Компилаторът разбира, че има 2 реда
// ⚠️ ВАЖНО: Броят КОЛОНИ трябва винаги да е зададен!
```

---

## 2) Достъп до елементи

### Индексиране:

```cpp
matrix[ред][колона]
```

**⚠️ Важно:** Индексирането започва от 0!

```cpp
int matrix[3][3] = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};

// Достъп до елементи:
int a = matrix[0][0];  // 1 (първи ред, първа колона)
int b = matrix[0][2];  // 3 (първи ред, трета колона)
int c = matrix[1][1];  // 5 (втори ред, втора колона)
int d = matrix[2][2];  // 9 (трети ред, трета колона)

// Промяна на елементи:
matrix[1][1] = 100;
// Сега матрицата е:
// 1   2   3
// 4  100  6
// 7   8   9
```

### Визуализация на индексите:

```
       колона 0   колона 1   колона 2
ред 0    [0][0]     [0][1]     [0][2]
ред 1    [1][0]     [1][1]     [1][2]
ред 2    [2][0]     [2][1]     [2][2]
```

---

## 3) Въвеждане и извеждане на матрица

### Въвеждане (вложени цикли):

```cpp
const int MAX_ROWS = 100;
const int MAX_COLS = 100;

int matrix[MAX_ROWS][MAX_COLS];
int rows, cols;

std::cout << "Enter rows and cols: ";
std::cin >> rows >> cols;

std::cout << "Enter matrix elements:\n";
for(int i = 0; i < rows; i++) {
    for(int j = 0; j < cols; j++) {
        std::cin >> matrix[i][j];
    }
}
```

### Извеждане:

```cpp
std::cout << "Matrix:\n";
for(int i = 0; i < rows; i++) {
    for(int j = 0; j < cols; j++) {
        std::cout << matrix[i][j] << " ";
    }
    std::cout << std::endl;  // Нов ред след всеки ред от матрицата
}
```

### Форматирано извеждане (подравнени колони):

```cpp
#include <iomanip>

for(int i = 0; i < rows; i++) {
    for(int j = 0; j < cols; j++) {
        std::cout << std::setw(5) << matrix[i][j];  // Ширина 5 символа
    }
    std::cout << std::endl;
}
```

---

## 4) Как се съхраняват матриците в паметта?

### ⚡ Row-major order (ред-основно подреждане)

В C++ двумерните масиви се съхраняват **последователно в паметта по редове**.

```cpp
int matrix[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
```

**Визуално виждаме:**

```
1 2 3
4 5 6
```

**Реално в паметта:**

```
[1] [2] [3] [4] [5] [6]
 ↑   ↑   ↑   ↑   ↑   ↑
row0      row1
```

### Изчисляване на адреса на елемент:

Формула за адрес на `matrix[i][j]`:

```
address = base_address + (i * num_cols + j) * sizeof(element_type)
```

**Пример:**

```cpp
int matrix[3][4];  // 3 реда, 4 колони
// Адрес на matrix[1][2]:
// base + (1 * 4 + 2) * sizeof(int) = base + 6 * 4 = base + 24 bytes
```

### 💡 Интересен факт:

Заради row-major подредбата, **обхождането по редове е по-бързо** от обхождането по колони (по-добър cache locality)!

```cpp
// ✅ БЪРЗО (cache-friendly):
for(int i = 0; i < rows; i++) {
    for(int j = 0; j < cols; j++) {
        process(matrix[i][j]);
    }
}

// ❌ ПО-БАВНО (cache-unfriendly):
for(int j = 0; j < cols; j++) {
    for(int i = 0; i < rows; i++) {
        process(matrix[i][j]);
    }
}
```

---

## 5) Предаване на матрици към функции

### ⚠️ ВАЖНО ПРАВИЛО:

При предаване на матрица като параметър, **ТРЯБВА** да зададете броя колони (всички измерения освен първото)!

### Правилно предаване:

```cpp
void printMatrix(int matrix[][3], int rows) {
    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < 3; j++) {
            std::cout << matrix[i][j] << " ";
        }
        std::cout << std::endl;
    }
}

int main() {
    int matrix[2][3] = {
        {1, 2, 3},
        {4, 5, 6}
    };

    printMatrix(matrix, 2);
    return 0;
}
```

### Защо трябва да зададем броя колони?

Компилаторът трябва да знае как да изчисли адреса на `matrix[i][j]`:

```
address = base + (i * COLS + j) * sizeof(int)
```

Без `COLS` не може да изчисли този адрес!

### Алтернативен подход с константа:

```cpp
const int MAX_COLS = 100;

void processMatrix(int matrix[][MAX_COLS], int rows, int cols) {
    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < cols; j++) {
            // Обработка
        }
    }
}
```

---

## 6) Обхождане на матрица - Различни pattern-и

### Pattern 1: По редове (стандартно)

```cpp
for(int i = 0; i < rows; i++) {
    for(int j = 0; j < cols; j++) {
        // Обработка на matrix[i][j]
    }
}
```

### Pattern 2: По колони

```cpp
for(int j = 0; j < cols; j++) {
    for(int i = 0; i < rows; i++) {
        // Обработка на matrix[i][j]
    }
}
```

### Pattern 3: Главен диагонал (квадратна матрица)

```cpp
// За квадратна матрица n x n
for(int i = 0; i < n; i++) {
    // matrix[i][i] са елементите на главния диагонал
}
```

### Pattern 4: Второстепенен диагонал

```cpp
for(int i = 0; i < n; i++) {
    // matrix[i][n - 1 - i]
}
```

### Pattern 5: Над главния диагонал

```cpp
for(int i = 0; i < n; i++) {
    for(int j = i + 1; j < n; j++) {
        // matrix[i][j] са елементите над диагонала
    }
}
```

### Pattern 6: Под главния диагонал

```cpp
for(int i = 1; i < n; i++) {
    for(int j = 0; j < i; j++) {
        // matrix[i][j] са елементите под диагонала
    }
}
```

### Pattern 7: Периметър (рамка)

```cpp
// Горен ред
for(int j = 0; j < cols; j++) {
    // matrix[0][j]
}

// Долен ред
for(int j = 0; j < cols; j++) {
    // matrix[rows-1][j]
}

// Лява колона (без ъглите)
for(int i = 1; i < rows - 1; i++) {
    // matrix[i][0]
}

// Дясна колона (без ъглите)
for(int i = 1; i < rows - 1; i++) {
    // matrix[i][cols-1]
}
```

---

## 7) Специални видове матрици

### 🔹 Квадратна матрица

Брой редове = брой колони

```cpp
int matrix[5][5];  // 5 x 5 квадратна матрица
```

### 🔹 Симетрична матрица

`matrix[i][j] == matrix[j][i]` за всички i, j

```cpp
int matrix[3][3] = {
    {1, 2, 3},
    {2, 5, 6},
    {3, 6, 9}
};
// matrix[0][1] == matrix[1][0] = 2
// matrix[0][2] == matrix[2][0] = 3
// matrix[1][2] == matrix[2][1] = 6
```

### 🔹 Диагонална матрица

Всички елементи извън главния диагонал са 0

```cpp
int matrix[3][3] = {
    {5, 0, 0},
    {0, 3, 0},
    {0, 0, 7}
};
```

### 🔹 Единична матрица (Identity matrix)

Диагоналните елементи са 1, останалите - 0

```cpp
int identity[3][3] = {
    {1, 0, 0},
    {0, 1, 0},
    {0, 0, 1}
};
```

### 🔹 Горна триъгълна матрица

Всички елементи **под** главния диагонал са 0

```cpp
int matrix[3][3] = {
    {1, 2, 3},
    {0, 4, 5},
    {0, 0, 6}
};
```

### 🔹 Долна триъгълна матрица

Всички елементи **над** главния диагонал са 0

```cpp
int matrix[3][3] = {
    {1, 0, 0},
    {2, 3, 0},
    {4, 5, 6}
};
```

---

## 8) Основни операции с матрици

### 🔸 Транспониране

Размяна на редове и колони: `result[j][i] = matrix[i][j]`

```cpp
void transpose(int matrix[][MAX], int result[][MAX], int rows, int cols) {
    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
}

// Пример:
// Оригинал:      Транспонирана:
// 1 2 3          1 4
// 4 5 6          2 5
//                3 6
```

### 🔸 Събиране на матрици

```cpp
void addMatrices(int A[][MAX], int B[][MAX], int result[][MAX],
                 int rows, int cols) {
    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < cols; j++) {
            result[i][j] = A[i][j] + B[i][j];
        }
    }
}
```

### 🔸 Умножение на матрица със скалар

```cpp
void multiplyByScalar(int matrix[][MAX], int rows, int cols, int scalar) {
    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < cols; j++) {
            matrix[i][j] *= scalar;
        }
    }
}
```

### 🔸 Умножение на матрици

За да умножим матрица A (m×n) с матрица B (n×p), резултатът е матрица C (m×p).

```cpp
void multiplyMatrices(int A[][MAX], int B[][MAX], int C[][MAX],
                      int m, int n, int p) {
    // C = A * B
    // A е m x n, B е n x p, C ще е m x p

    for(int i = 0; i < m; i++) {
        for(int j = 0; j < p; j++) {
            C[i][j] = 0;
            for(int k = 0; k < n; k++) {
                C[i][j] += A[i][k] * B[k][j];
            }
        }
    }
}
```

**Пример:**

```
A (2x3):        B (3x2):        C (2x2):
1 2 3           7  8            58  64
4 5 6           9 10            139 154
               11 12
```

---

## 9) Често използвани алгоритми

### 🔸 Намиране на максимален елемент и позицията му

```cpp
void findMax(int matrix[][MAX], int rows, int cols) {
    int maxVal = matrix[0][0];
    int maxRow = 0, maxCol = 0;

    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < cols; j++) {
            if(matrix[i][j] > maxVal) {
                maxVal = matrix[i][j];
                maxRow = i;
                maxCol = j;
            }
        }
    }

    std::cout << "Max: " << maxVal
              << " at [" << maxRow << "][" << maxCol << "]\n";
}
```

### 🔸 Сума на всички елементи

```cpp
int sumAll(int matrix[][MAX], int rows, int cols) {
    int sum = 0;
    for(int i = 0; i < rows; i++) {
        for(int j = 0; j < cols; j++) {
            sum += matrix[i][j];
        }
    }
    return sum;
}
```

### 🔸 Сума по редове

```cpp
void sumRows(int matrix[][MAX], int rows, int cols) {
    for(int i = 0; i < rows; i++) {
        int rowSum = 0;
        for(int j = 0; j < cols; j++) {
            rowSum += matrix[i][j];
        }
        std::cout << "Row " << i << " sum: " << rowSum << std::endl;
    }
}
```

### 🔸 Сума по колони

```cpp
void sumCols(int matrix[][MAX], int rows, int cols) {
    for(int j = 0; j < cols; j++) {
        int colSum = 0;
        for(int i = 0; i < rows; i++) {
            colSum += matrix[i][j];
        }
        std::cout << "Col " << j << " sum: " << colSum << std::endl;
    }
}
```

### 🔸 Проверка дали матрицата е симетрична

```cpp
bool isSymmetric(int matrix[][MAX], int n) {
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            if(matrix[i][j] != matrix[j][i]) {
                return false;
            }
        }
    }
    return true;
}
```

---

## 10) Тримерни масиви

Тримерните масиви са **масиви от двумерни масиви**. Представете си ги като стек от матрици.

### Деклариране:

```cpp
int cube[3][4][5];  // 3 матрици, всяка 4x5
```

### Визуализация:

```
Слой 0:        Слой 1:        Слой 2:
[][][]...      [][][]...      [][][]...
[][][]...      [][][]...      [][][]...
[][][]...      [][][]...      [][][]...
[][][]...      [][][]...      [][][]...
```

### Достъп:

```cpp
cube[слой][ред][колона]
```

### Инициализация:

```cpp
int cube[2][2][3] = {
    {  // Слой 0
        {1, 2, 3},
        {4, 5, 6}
    },
    {  // Слой 1
        {7, 8, 9},
        {10, 11, 12}
    }
};
```

### Обхождане:

```cpp
for(int layer = 0; layer < 2; layer++) {
    for(int i = 0; i < 2; i++) {
        for(int j = 0; j < 3; j++) {
            std::cout << cube[layer][i][j] << " ";
        }
        std::cout << std::endl;
    }
    std::cout << "---\n";
}
```

---

## 11) Често срещани грешки и как да ги избегнем

### ❌ Грешка 1: Объркване на индекси

```cpp
// ГРЕШНО:
int matrix[3][4];
matrix[4][2] = 10;  // Извън границите! (редовете са 0-2)

// ПРАВИЛНО:
matrix[2][3] = 10;  // Последен ред, последна колона
```

### ❌ Грешка 2: Объркване на rows и cols

```cpp
// ГРЕШНО:
for(int i = 0; i < cols; i++) {
    for(int j = 0; j < rows; j++) {
        matrix[i][j] = 0;  // Може да излезе извън границите!
    }
}

// ПРАВИЛНО:
for(int i = 0; i < rows; i++) {
    for(int j = 0; j < cols; j++) {
        matrix[i][j] = 0;
    }
}
```

### ❌ Грешка 3: Липсващ брой колони при функция

```cpp
// ГРЕШНО:
void func(int matrix[][], int rows, int cols) { }  // ❌ Няма да компилира!

// ПРАВИЛНО:
void func(int matrix[][100], int rows, int cols) { }
```

### ❌ Грешка 4: Неинициализирани елементи

```cpp
int matrix[3][3];  // Съдържа боклук!

// По-добре:
int matrix[3][3] = {0};  // Всички елементи са 0
```

### ❌ Грешка 5: Объркване при транспониране

```cpp
// ГРЕШНО (презаписва оригинала некоректно):
for(int i = 0; i < n; i++) {
    for(int j = 0; j < n; j++) {
        matrix[i][j] = matrix[j][i];  // ❌ Губим данни!
    }
}

// ПРАВИЛНО (размяна на елементи):
for(int i = 0; i < n; i++) {
    for(int j = i + 1; j < n; j++) {  // Само над диагонала
        int temp = matrix[i][j];
        matrix[i][j] = matrix[j][i];
        matrix[j][i] = temp;
    }
}
```

---

## 12) Интересни факти и трикове

### 💡 Факт 1: Размер в паметта

```cpp
int matrix[10][20];
sizeof(matrix);  // 10 * 20 * sizeof(int) = 10 * 20 * 4 = 800 bytes
```

### 💡 Факт 2: Матрица всъщност е едномерен масив

```cpp
int matrix[2][3];
// Същото като:
// int arr[6] с mapping: matrix[i][j] = arr[i * 3 + j]
```

### 💡 Факт 3: Бързо зануляване

```cpp
#include <cstring>

int matrix[100][100];
memset(matrix, 0, sizeof(matrix));  // Бързо зануляване на цялата матрица
```

**⚠️ Внимание:** `memset` работи само за 0 и -1 при масиви от `int`!

### 💡 Факт 4: Диагоналите в квадратна матрица

За матрица n×n:

- **Главен диагонал:** i == j
- **Второстепенен диагонал:** i + j == n - 1
- **Над главния диагонал:** i < j
- **Под главния диагонал:** i > j

---

## 13) Практически съвети

### ✅ DO (правете):

1. **Винаги** използвайте константи за максималния размер
2. **Проверявайте** границите преди достъп
3. **Инициализирайте** масивите преди употреба
4. **Коментирайте** сложни индексации
5. **Обхождайте по редове** за по-добра производителност

### ❌ DON'T (избягвайте):

1. **Не** забравяйте броя колони при предаване на функция
2. **Не** объркайте индексите i и j
3. **Не** достъпвайте елементи извън границите
4. **Не** използвайте `memset` за стойности различни от 0/-1
5. **Не** забравяйте, че индексите започват от 0

---

## 🎯 Обобщение

### Ключови точки:

1. Двумерният масив е **масив от масиви**
2. Индексиране: `matrix[ред][колона]`, започва от **[0][0]**
3. Съхранение в паметта: **row-major order**
4. При функции: **задължително** задавайте броя колони
5. Две вложени цикъла за пълно обхождане

### Често използвани операции:

| Операция          | Сложност | Описание             |
| ----------------- | -------- | -------------------- |
| Достъп до елемент | O(1)     | `matrix[i][j]`       |
| Обхождане         | O(n×m)   | Два вложени цикъла   |
| Транспониране     | O(n×m)   | Размяна ред↔колона   |
| Умножение         | O(n³)    | За квадратни матрици |
| Намиране макс/мин | O(n×m)   | Обхождане на всички  |

---

**Успех с упражненията! 🚀**
