# **ЗАДАЧИ - СЕДМИЦА 11**

## **Адресна аритметика - Основни задачи**

---

## **ЗАДАЧА 0: Warming Up - Какво ще се изведе?**

Без да пускате програмата, определете какво ще се изведе:

```cpp
int arr[5] = {10, 20, 30, 40, 50};
int* ptr = arr;

std::cout << *ptr << std::endl;
std::cout << *(ptr + 2) << std::endl;
std::cout << ptr[3] << std::endl;
```

<details>
<summary><b>📝 Отговор</b></summary>

```
10
30
40
```

**Обяснение:**
- `*ptr` → стойността на първия елемент → 10
- `*(ptr + 2)` → стойността на третия елемент (ptr + 2) → 30
- `ptr[3]` → стойността на четвъртия елемент → 40

</details>

---

## **ЗАДАЧА 1: Изчисляване на адреси**

Даден е следният код:

```cpp
int arr[6] = {5, 10, 15, 20, 25, 30};
// Нека адресът на arr[0] е 0x1000
```

Попълнете таблицата:

| Израз | Адрес | Стойност |
|-------|-------|----------|
| arr | ? | ? |
| arr + 1 | ? | ? |
| arr + 3 | ? | ? |
| &arr[4] | ? | ? |
| *arr | ? | ? |
| *(arr + 2) | ? | ? |
| arr[5] | ? | ? |

<details>
<summary><b>📝 Отговор</b></summary>

| Израз | Адрес | Стойност |
|-------|-------|----------|
| arr | 0x1000 | 0x1000 |
| arr + 1 | 0x1004 | 0x1004 |
| arr + 3 | 0x100C | 0x100C |
| &arr[4] | 0x1010 | 0x1010 |
| *arr | - | 5 |
| *(arr + 2) | - | 15 |
| arr[5] | - | 30 |

**Обяснение:**
- Всеки `int` заема 4 bytes
- `arr + n` увеличава адреса с `n × 4` bytes
- `*` дава стойност, а не адрес

</details>

---

## **ЗАДАЧА 2: Обхождане с указател**

Напишете функция `printArrayWithPointer`, която приема масив и размер, и го отпечатва използвайки **само адресна аритметика** (без индекси `[]`).

```cpp
// Вход:
int arr[5] = {1, 2, 3, 4, 5};
printArrayWithPointer(arr, 5);

// Изход:
1 2 3 4 5
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
void printArrayWithPointer(int* arr, int size) {
    int* end = arr + size;
    
    for (int* ptr = arr; ptr < end; ptr++) {
        std::cout << *ptr << " ";
    }
    std::cout << std::endl;
}

// Алтернативно решение:
void printArrayWithPointer(int* arr, int size) {
    for (int i = 0; i < size; i++) {
        std::cout << *(arr + i) << " ";
    }
    std::cout << std::endl;
}
```

</details>

---

## **ЗАДАЧА 3: Сума с указатели**

Напишете функция `sumWithPointers`, която изчислява сумата на елементите на масив използвайки адресна аритметика.

```cpp
// Вход:
int arr[5] = {10, 20, 30, 40, 50};
int sum = sumWithPointers(arr, 5);

// Изход:
150
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int sumWithPointers(int* arr, int size) {
    int sum = 0;
    int* end = arr + size;
    
    for (int* ptr = arr; ptr < end; ptr++) {
        sum += *ptr;
    }
    
    return sum;
}

// Алтернативно (без временна променлива end):
int sumWithPointers(int* arr, int size) {
    int sum = 0;
    
    for (int* ptr = arr; ptr < arr + size; ptr++) {
        sum += *ptr;
    }
    
    return sum;
}
```

</details>

---

## **ЗАДАЧА 4: Намиране на минимум**

Напишете функция `findMin`, която намира минималния елемент в масив използвайки указатели.

```cpp
// Вход:
int arr[6] = {45, 12, 67, 8, 34, 23};
int min = findMin(arr, 6);

// Изход:
8
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int findMin(int* arr, int size) {
    int min = *arr;  // Първият елемент
    
    for (int* ptr = arr + 1; ptr < arr + size; ptr++) {
        if (*ptr < min) {
            min = *ptr;
        }
    }
    
    return min;
}
```

**Обяснение:**
- Започваме от `arr + 1` (вторият елемент), защото първият вече го имаме
- Сравняваме всеки елемент с текущия минимум

</details>

---

## **ЗАДАЧА 5: Броене на четни числа**

Напишете функция `countEven`, която брои четните числа в масив използвайки адресна аритметика.

```cpp
// Вход:
int arr[7] = {1, 2, 3, 4, 5, 6, 7};
int count = countEven(arr, 7);

// Изход:
3
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int countEven(int* arr, int size) {
    int count = 0;
    
    for (int* ptr = arr; ptr < arr + size; ptr++) {
        if (*ptr % 2 == 0) {
            count++;
        }
    }
    
    return count;
}
```

</details>

---

## **ЗАДАЧА 6: Обръщане на масив**

Напишете функция `reverseArray`, която обръща елементите на масив на място (in-place) използвайки два указателя.

```cpp
// Вход:
int arr[5] = {1, 2, 3, 4, 5};
reverseArray(arr, 5);

// След изпълнение:
[5, 4, 3, 2, 1]
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
void reverseArray(int* arr, int size) {
    int* left = arr;
    int* right = arr + size - 1;
    
    while (left < right) {
        // Размяна
        int temp = *left;
        *left = *right;
        *right = temp;
        
        left++;
        right--;
    }
}
```

**Визуализация:**

```
Начало:
left                    right
 ↓                        ↓
[1]  [2]  [3]  [4]  [5]

След размяна:
    left            right
      ↓              ↓
[5]  [2]  [3]  [4]  [1]

След размяна:
         left
        right
          ↓
[5]  [4]  [3]  [2]  [1]
```

</details>

---

## **ЗАДАЧА 7: Копиране на масив**

Напишете функция `copyArray`, която копира елементите от един масив в друг използвайки указатели.

```cpp
// Вход:
int source[5] = {10, 20, 30, 40, 50};
int destination[5];
copyArray(source, destination, 5);

// След изпълнение:
destination: [10, 20, 30, 40, 50]
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
void copyArray(int* source, int* destination, int size) {
    int* src_end = source + size;
    
    while (source < src_end) {
        *destination = *source;
        source++;
        destination++;
    }
}

// Компактна версия:
void copyArray(int* source, int* destination, int size) {
    for (int* end = source + size; source < end; ) {
        *destination++ = *source++;
    }
}
```

**Обяснение на `*destination++ = *source++;`:**
1. `*source` → вземи стойността от source
2. `*destination = ...` → запиши я в destination
3. `source++` → премести source напред
4. `destination++` → премести destination напред

</details>

---

## **ЗАДАЧА 8: Умножаване на елементи по константа**

Напишете функция `multiplyArray`, която умножава всички елементи на масив по дадена константа.

```cpp
// Вход:
int arr[4] = {1, 2, 3, 4};
multiplyArray(arr, 4, 5);

// След изпълнение:
[5, 10, 15, 20]
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
void multiplyArray(int* arr, int size, int multiplier) {
    int* end = arr + size;
    
    for (int* ptr = arr; ptr < end; ptr++) {
        *ptr *= multiplier;
    }
}
```

</details>

---

## **ЗАДАЧА 9: Намиране на позиция**

Напишете функция `findPosition`, която намира индекса на първото срещане на дадена стойност в масив. Ако стойността не е намерена, връща -1.

Използвайте **само адресна аритметика** (без индекси).

```cpp
// Вход:
int arr[6] = {10, 20, 30, 40, 50, 60};
int pos = findPosition(arr, 6, 40);

// Изход:
3
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int findPosition(int* arr, int size, int value) {
    for (int* ptr = arr; ptr < arr + size; ptr++) {
        if (*ptr == value) {
            return ptr - arr;  // Изваждане на указатели!
        }
    }
    return -1;  // Не е намерен
}
```

**Обяснение:**
- `ptr - arr` дава разстоянието между `ptr` и началото на масива
- Това е точно индексът на елемента!

</details>

---

## **ЗАДАЧА 10: Проследяване на указател**

Определете какво ще съдържа масивът след изпълнение на кода:

```cpp
int arr[5] = {10, 20, 30, 40, 50};
int* ptr = arr + 2;

*ptr = 100;
ptr++;
*ptr = 200;
ptr--;
ptr--;
*ptr = 300;

// Какъв е масивът в края?
```

<details>
<summary><b>📝 Отговор</b></summary>

```cpp
[10, 300, 100, 200, 50]
```

**Стъпка по стъпка:**

```
Начало: [10, 20, 30, 40, 50]
         ptr = arr + 2 (сочи към 30)

*ptr = 100:  [10, 20, 100, 40, 50]
                      ↑
                     ptr

ptr++:       [10, 20, 100, 40, 50]
                          ↑
                         ptr

*ptr = 200:  [10, 20, 100, 200, 50]
                          ↑
                         ptr

ptr--:       [10, 20, 100, 200, 50]
                      ↑
                     ptr

ptr--:       [10, 20, 100, 200, 50]
                  ↑
                 ptr

*ptr = 300:  [10, 300, 100, 200, 50]
                  ↑
                 ptr
```

</details>

---

## **ЗАДАЧА 11: Размяна на два елемента**

Напишете функция `swapElements`, която разменя два елемента на дадени позиции в масив използвайки указатели.

```cpp
// Вход:
int arr[5] = {1, 2, 3, 4, 5};
swapElements(arr, 1, 3);  // Размяна на позиции 1 и 3

// След изпълнение:
[1, 4, 3, 2, 5]
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
void swapElements(int* arr, int pos1, int pos2) {
    int* ptr1 = arr + pos1;
    int* ptr2 = arr + pos2;
    
    int temp = *ptr1;
    *ptr1 = *ptr2;
    *ptr2 = temp;
}

// Алтернативно (по-директно):
void swapElements(int* arr, int pos1, int pos2) {
    int temp = *(arr + pos1);
    *(arr + pos1) = *(arr + pos2);
    *(arr + pos2) = temp;
}
```

</details>

---

## **ЗАДАЧА 12: Сравнение на два масива**

Напишете функция `arraysEqual`, която проверява дали два масива са еднакви (съдържат едни и същи елементи на едни и същи позиции).

```cpp
// Вход:
int arr1[5] = {1, 2, 3, 4, 5};
int arr2[5] = {1, 2, 3, 4, 5};
bool equal = arraysEqual(arr1, arr2, 5);

// Изход:
true
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
bool arraysEqual(int* arr1, int* arr2, int size) {
    int* end = arr1 + size;
    
    for (; arr1 < end; arr1++, arr2++) {
        if (*arr1 != *arr2) {
            return false;
        }
    }
    
    return true;
}

// Алтернативно:
bool arraysEqual(int* arr1, int* arr2, int size) {
    for (int i = 0; i < size; i++) {
        if (*(arr1 + i) != *(arr2 + i)) {
            return false;
        }
    }
    return true;
}
```

</details>

---

## **ЗАДАЧА 13: Динамична памет с адресна аритметика**

Напишете функция, която създава динамичен масив, попълва го с квадрати на числата от 1 до n, и го връща.

```cpp
// Вход:
int* result = createSquares(5);

// Резултат:
[1, 4, 9, 16, 25]

// ⚠️ НЕ ЗАБРАВЯЙТЕ delete[]!
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int* createSquares(int n) {
    int* arr = new int[n];
    
    for (int* ptr = arr; ptr < arr + n; ptr++) {
        int index = ptr - arr;  // Изчисляваме индекса
        *ptr = (index + 1) * (index + 1);
    }
    
    return arr;
}

// Използване:
int main() {
    int* squares = createSquares(5);
    
    // Отпечатване
    for (int i = 0; i < 5; i++) {
        std::cout << *(squares + i) << " ";
    }
    std::cout << std::endl;
    
    delete[] squares;  // ⚠️ ВАЖНО!
    return 0;
}
```

</details>

---

## **ЗАДАЧА 14: Филтриране на положителни числа**

Напишете функция `filterPositive`, която копира само положителните числа от един масив в друг. Функцията връща броя на копираните елементи.

```cpp
// Вход:
int source[7] = {-5, 10, -3, 20, 0, 30, -15};
int destination[7];
int count = filterPositive(source, destination, 7);

// destination съдържа: [10, 20, 30, ?, ?, ?, ?]
// count = 3
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int filterPositive(int* source, int* destination, int size) {
    int* dest_ptr = destination;
    int* src_end = source + size;
    
    for (int* src_ptr = source; src_ptr < src_end; src_ptr++) {
        if (*src_ptr > 0) {
            *dest_ptr = *src_ptr;
            dest_ptr++;
        }
    }
    
    return dest_ptr - destination;  // Брой копирани елементи
}
```

**Обяснение:**
- `dest_ptr` се движи само когато копираме елемент
- В края `dest_ptr - destination` ни дава броя копирани елементи

</details>

---

## **ЗАДАЧА 15: Намиране на максимум с адрес**

Напишете функция `findMaxPointer`, която връща **указател** към максималния елемент в масив (не стойността, а адреса).

```cpp
// Вход:
int arr[5] = {10, 50, 30, 20, 40};
int* maxPtr = findMaxPointer(arr, 5);

std::cout << *maxPtr << std::endl;  // 50
std::cout << (maxPtr - arr) << std::endl;  // 1 (индексът)
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
int* findMaxPointer(int* arr, int size) {
    int* maxPtr = arr;
    
    for (int* ptr = arr + 1; ptr < arr + size; ptr++) {
        if (*ptr > *maxPtr) {
            maxPtr = ptr;
        }
    }
    
    return maxPtr;
}

// Използване:
int main() {
    int arr[5] = {10, 50, 30, 20, 40};
    int* maxPtr = findMaxPointer(arr, 5);
    
    std::cout << "Max value: " << *maxPtr << std::endl;
    std::cout << "Position: " << (maxPtr - arr) << std::endl;
    
    // Можем дори да променим максималния елемент:
    *maxPtr = 100;
    // arr сега е: [10, 100, 30, 20, 40]
    
    return 0;
}
```

</details>

---

## **ЗАДАЧА 16: Палиндром проверка**

Напишете функция `isPalindrome`, която проверява дали масив е палиндром (чете се еднакво отляво надясно и отдясно наляво) използвайки два указателя.

```cpp
// Вход:
int arr1[5] = {1, 2, 3, 2, 1};
bool result1 = isPalindrome(arr1, 5);  // true

int arr2[5] = {1, 2, 3, 4, 5};
bool result2 = isPalindrome(arr2, 5);  // false
```

<details>
<summary><b>📝 Решение</b></summary>

```cpp
bool isPalindrome(int* arr, int size) {
    int* left = arr;
    int* right = arr + size - 1;
    
    while (left < right) {
        if (*left != *right) {
            return false;
        }
        left++;
        right--;
    }
    
    return true;
}
```

**Визуализация:**

```
Масив: [1, 2, 3, 2, 1]

Стъпка 1:
left              right
 ↓                  ↓
[1]  [2]  [3]  [2]  [1]
 1 == 1 ✅

Стъпка 2:
    left      right
      ↓        ↓
[1]  [2]  [3]  [2]  [1]
     2 == 2 ✅

Стъпка 3:
         left
        right
          ↓
[1]  [2]  [3]  [2]  [1]
left >= right → СТОП → true
```

</details>

---

**Успех с адресната аритметика! 🚀**

_Следващи стъпки: TasksForHome.md за допълнителна практика_
