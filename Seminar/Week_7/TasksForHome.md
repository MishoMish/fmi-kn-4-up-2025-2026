# **ЗАДАЧИ ЗА ДОМАШНА РАБОТА - СЕДМИЦА 7**

## **Символни низове - Упражнения за затвърдяване**

---

## 📚 Основни задачи

---

## Задача 1: Сортиране на символи в низ

Напишете програма, която приема от стандартния вход символен низ и го сортира по азбучен ред (по ASCII стойност). След това извежда сортирания низ в конзолата.

```c++
// Вход:
dcbaFMI

// Изход:
FIMabcd
```

**Допълнителни тестове:**

```c++
// Вход: "hello world"
// Изход: " dehllloorw"

// Вход: "321ABC"
// Изход: "123ABC"
```

> **Подсказки:**
>
> - Можете да използвате Bubble Sort или Selection Sort
> - ASCII стойностите: цифри < главни букви < малки букви
> - Интервалите и специалните символи също се сортират

---

## Задача 2: Сортиране на три низа

Напишете програма, която приема от конзолата 3 символни низа и ги подрежда в лексикографски (азбучен) ред.

```c++
// Вход:
Zebra
Apple
Banana

// Изход:
Apple Banana Zebra
```

**Допълнителни тестове:**

```c++
// Вход: "xyz", "abc", "mno"
// Изход: "abc mno xyz"

// Вход: "Dog", "dog", "DOG"
// Изход: "DOG Dog dog"
```

> **Подсказки:**
>
> - Използвайте `strcmp()` за сравнение на низове
> - `strcmp(str1, str2) < 0` означава, че str1 е "по-малък" (идва преди str2)
> - Сортирайте с прост алгоритъм (напр. bubble sort за 3 елемента)

---

## Задача 3: Кодиране и декодиране на три низа

Напишете програма, която приема три символни низа с **еднаква дължина** n.

1. Проверете дали трите низа имат еднаква дължина (ако не - изведете грешка)
2. Реализирайте функция `encode()` - която "смесва" символите от трите низа: взема първия символ от всеки низ, после втория от всеки низ и т.н.
3. Реализирайте функция `decode()` - която разделя смесения низ обратно на трите оригинални низа

```c++
// Вход:
3           // n (дължина на всеки низ)
abc         // charArr1
123         // charArr2
xyz         // charArr3

// Изход на encode():
a1xb2yc3z   // charArrMixed

// Изход на decode(charArrMixed):
abc
123
xyz
```

**Допълнителни тестове:**

```c++
// Вход: n=4, "ABCD", "1234", "wxyz"
// encode() → "A1wB2xC3yD4z"
// decode() → "ABCD", "1234", "wxyz"

// Вход: n=2, "Hi", "!!!", "99"
// encode() → "H!9i!9"
// decode() → "Hi", "!!!", "99"
```

**Сигнатури:**

```c++
void encode(const char* str1, const char* str2, const char* str3, char* result);
void decode(const char* mixed, char* str1, char* str2, char* str3, int n);
```

> **Подсказки:**
>
> - При encode: обходете индекс i от 0 до n-1, добавяйте str1[i], str2[i], str3[i]
> - При decode: обходете mixed на стъпка от 3 (позиции 0,3,6,... за str1; 1,4,7,... за str2; 2,5,8,... за str3)

---

## 🚀 Средно трудни задачи

---

## Задача 4: Премахване на гласни

Напишете функция `removeVowels`, която премахва всички гласни букви от низ (a, e, i, o, u, A, E, I, O, U).

```c++
char text1[] = "Hello World";
removeVowels(text1);
// text1 = "Hll Wrld"

char text2[] = "Programming";
removeVowels(text2);
// text2 = "Prgrmmng"

char text3[] = "AEIOU";
removeVowels(text3);
// text3 = ""
```

**Сигнатура:**

```c++
void removeVowels(char* str);
```

---

## Задача 5: Обръщане на думите в изречение

Напишете функция `reverseWords`, която обръща реда на думите в изречение (думите са разделени с интервали).

```c++
char text1[] = "Hello World";
reverseWords(text1);
// text1 = "World Hello"

char text2[] = "I love FMI";
reverseWords(text2);
// text2 = "FMI love I"

char text3[] = "one";
reverseWords(text3);
// text3 = "one"
```

**Сигнатура:**

```c++
void reverseWords(char* str);
```

> **Подсказка:**
>
> 1. Обърнете целия низ
> 2. Обърнете всяка дума поотделно

---

## Задача 6: Най-честа буква

Напишете функция `mostFrequentChar`, която намира най-често срещаната буква в низ (case-insensitive, т.е. 'A' и 'a' се броят заедно).

```c++
mostFrequentChar("Hello World");     // 'l' (среща се 3 пъти)
mostFrequentChar("Programming");     // 'g' или 'r' или 'm' (всички се срещат по 2 пъти)
mostFrequentChar("AAaaa");           // 'a' (5 пъти общо)
```

**Сигнатура:**

```c++
char mostFrequentChar(const char* str);
```

> **Подсказки:**
>
> - Създайте масив за честотите на буквите (26 елемента за a-z)
> - Преобразувайте всички букви в малки преди броене
> - Намерете максимума в масива с честоти

---

## 🔥 Трудни задачи

---

## Задача 7: Валидация на имейл

Напишете функция `isValidEmail`, която проверява дали даден низ е валиден имейл адрес. Критерии:

- Съдържа точно една '@'
- Има поне един символ преди '@'
- Има поне един символ след '@'
- Съдържа поне една '.' след '@'
- Има поне един символ след последната '.'

```c++
isValidEmail("user@example.com");       // true
isValidEmail("test.user@fmi.bg");       // true
isValidEmail("invalid");                // false (няма @)
isValidEmail("@example.com");           // false (няма символи преди @)
isValidEmail("user@");                  // false (няма символи след @)
isValidEmail("user@domain");            // false (няма . след @)
isValidEmail("user@domain.");           // false (няма символи след .)
```

**Сигнатура:**

```c++
bool isValidEmail(const char* email);
```

---

## Задача 8: Римски числа

Напишете функция `romanToInt`, която конвертира римско число (като символен низ) в цяло число.

**Римски цифри:**

- I = 1
- V = 5
- X = 10
- L = 50
- C = 100
- D = 500
- M = 1000

**Правила:**

- Когато по-малка цифра е преди по-голяма, изваждаме малката (напр. IV = 4, IX = 9)
- Иначе събираме (напр. VI = 6, XI = 11)

```c++
romanToInt("III");       // 3
romanToInt("IV");        // 4
romanToInt("IX");        // 9
romanToInt("LVIII");     // 58
romanToInt("MCMXCIV");   // 1994
```

**Сигнатура:**

```c++
int romanToInt(const char* roman);
```

> **Подсказка:**
>
> - Обходете низа отляво надясно
> - Ако текущата цифра е по-малка от следващата → извади
> - Иначе → добави

---

## Задача 9: Проверка за изоморфни низове

Напишете функция `areIsomorphic`, която проверява дали два низа са изоморфни. Два низа са изоморфни, ако има взаимно-еднозначно съответствие между символите им.

```c++
areIsomorphic("egg", "add");        // true ('e'→'a', 'g'→'d')
areIsomorphic("foo", "bar");        // false ('o' мапва към 'a' и 'r')
areIsomorphic("paper", "title");    // true
areIsomorphic("abc", "def");        // true
areIsomorphic("ab", "aa");          // false
```

**Сигнатура:**

```c++
bool areIsomorphic(const char* str1, const char* str2);
```

> **Подсказка:**
>
> - Използвайте два масива заMapping (str1→str2 и str2→str1)
> - Проверявайте дали съответствието е консистентно

---

## 💎 Бонус задачи (за много напреднали)

---

## Бонус 1: RLE (Run-Length Encoding) Компресия и Декомпресия

Реализирайте две функции - `compress` и `decompress`, които правят RLE компресия.

### `compress()` - Компресира низ

```c++
char text1[] = "aaabbc";
char result1[100];
compress(text1, result1);
// result1 = "a3b2c1"

char text2[] = "aabbccdd";
char result2[100];
compress(text2, result2);
// result2 = "a2b2c2d2"
```

### `decompress()` - Декомпресира низ

```c++
char compressed1[] = "a3b2c1";
char result1[100];
decompress(compressed1, result1);
// result1 = "aaabbc"

char compressed2[] = "x5y2";
char result2[100];
decompress(compressed2, result2);
// result2 = "xxxxxyy"
```

**Сигнатури:**

```c++
void compress(const char* str, char* result);
void decompress(const char* str, char* result);
```

---

## Бонус 2: Проверка за подниз

Напишете функция `isSubstring`, която проверява дали `pattern` е подниз на `text` (без да използвате `strstr`).

```c++
isSubstring("Hello World", "World");    // true
isSubstring("FMI", "M");                // true
isSubstring("Programming", "gram");     // true
isSubstring("ABC", "AC");               // false
isSubstring("test", "testing");         // false
```

**Сигнатура:**

```c++
bool isSubstring(const char* text, const char* pattern);
```

---

## Бонус 3: Генериране на всички пермутации

Напишете **рекурсивна** функция, която извежда всички пермутации на даден низ.

```c++
printPermutations("ABC");
// Изход:
ABC
ACB
BAC
BCA
CAB
CBA

printPermutations("12");
// Изход:
12
21
```

**Сигнатура:**

```c++
void printPermutations(char* str, int start, int end);
```

> **Подсказка:** Използвайте backtracking - разменяйте елементи и рекурсивно викайте функцията.

---

## 📝 Указания за решаване

### Общи съвети:

1. **Четете условието внимателно** - обърнете внимание на edge cases
2. **Тествайте с различни входове** - празен низ, един символ, дълъг низ
3. **Не забравяйте '\0'** - след всяка модификация на низ
4. **Валидирайте входа** - проверявайте за NULL указатели, празни низове
5. **Коментирайте кода** - обяснете логиката на сложните части

### Как да тествате:

```c++
int main() {
    // Тест 1: Обикновен случай
    char test1[] = "Hello";
    yourFunction(test1);
    // Очакван резултат: ...

    // Тест 2: Празен низ
    char test2[] = "";
    yourFunction(test2);
    // Очакван резултат: ...

    // Тест 3: Един символ
    char test3[] = "A";
    yourFunction(test3);
    // Очакван резултат: ...

    return 0;
}
```

---

**Успех с домашните! 🎓💪**

> **Забележка:** Ако срещнете затруднения, прегледайте отново теорията и примерите от практикума. Винаги започвайте с по-простите задачи и постепенно преминавайте към по-сложните!
