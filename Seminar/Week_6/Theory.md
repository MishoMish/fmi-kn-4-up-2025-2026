# **CHEATSHEET - ПОДГОТОВКА ЗА ТЕСТ**

## **Теория и трикове за решаване на задачи**

---

## **1. ОСНОВИ - МАСИВИ**

### Деклариране и въвеждане на масив

```cpp
int n;
std::cin >> n;

int arr[100];  // Фиксиран размер (статична памет)
for(int i = 0; i < n; i++) {
    std::cin >> arr[i];
}
```

**⚠️ Важно:** Индексите започват от 0! Първият елемент е `arr[0]`, последният е `arr[n-1]`

### 💡 Ключова концепция: Фиксиран размер vs Реална дължина

**Идея:** Декларираме масив с **максимален размер**, но използваме само **n елемента**

```cpp
int arr[100];  // Масивът има място за 100 елемента
int n = 10;    // Но използваме само 10

// Въвеждаме само n елемента
for(int i = 0; i < n; i++) {
    std::cin >> arr[i];
}

// Обхождаме само n елемента
for(int i = 0; i < n; i++) {
    std::cout << arr[i] << " ";
}

// Останалите 90 елемента (arr[10] до arr[99]) НЕ НИ ИНТЕРЕСУВАТ!
```

**Защо го правим така?**

- ✅ Масивите в C++ трябва да имат **константен размер** при деклариране
- ✅ Не знаем предварително колко точно елемента ще има
- ✅ Декларираме **по-голям масив** (според максималното ограничение)
- ✅ Използваме **променливата n** за да контролираме реалната дължина

**Пример:**

```cpp
int arr[1000];  // Максимум 1000 елемента (от условието)
int n;
std::cin >> n;  // Въвеждаме реалния брой (например n = 50)

// Използваме само първите 50 елемента!
for(int i = 0; i < n; i++) {
    std::cin >> arr[i];
}

// arr[50] до arr[999] остават неинициализирани и НЕ ги използваме
```

**⚠️ Важни правила:**

1. Винаги декларирайте масив с **размер според максималното ограничение** от условието
2. Използвайте **n за реалната дължина** при циклите
3. **Никога не достъпвайте** елементи извън `[0, n-1]`
4. Останалите елементи **не ни интересуват** и не трябва да ги пипаме

### Обхождане на масив

```cpp
// Обхождане отляво надясно
for(int i = 0; i < n; i++) {
    // Работа с arr[i]
}

// Обхождане отдясно наляво
for(int i = n - 1; i >= 0; i--) {
    // Работа с arr[i]
}
```

---

## **2. СИМВОЛНИ НИЗОВЕ (char arrays)**

### Деклариране и четене

```cpp
char text[100];

// Четене на дума (без интервали)
std::cin >> text;

// Четене на цял ред (с интервали)
std::cin.getline(text, 100);
```

**⚠️ КРИТИЧНО:** Символните низове завършват с `'\0'` (терминираща нула)!

### Обхождане на символен низ

```cpp
// Докато не стигнем края
for(int i = 0; text[i] != '\0'; i++) {
    // Работа с text[i]
}

// Алтернативно - използвайте strlen (трябва #include <cstring>)
for(int i = 0; i < strlen(text); i++) {
    // Работа с text[i]
}
```

### Проверки за символи

```cpp
// Дали е буква (латиница)
bool isLetter = (c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z');

// Дали е главна буква
bool isUpper = (c >= 'A' && c <= 'Z');

// Дали е малка буква
bool isLower = (c >= 'a' && c <= 'z');

// Дали е цифра
bool isDigit = (c >= '0' && c <= '9');

// Дали е интервал
bool isSpace = (c == ' ');

// Дали е гласна буква
bool isVowel = (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' ||
                c == 'A' || c == 'E' || c == 'I' || c == 'O' || c == 'U');
```

---

## **3. PATTERN: БРОЕНЕ С УСЛОВИЕ**

### Броене на елементи с определено свойство

```cpp
int count = 0;
for(int i = 0; i < n; i++) {
    if(/* условие */) {
        count++;
    }
}
```

**Примери:**

```cpp
// Брой четни числа
int evenCount = 0;
for(int i = 0; i < n; i++) {
    if(arr[i] % 2 == 0) {
        evenCount++;
    }
}

// Брой положителни числа
int positiveCount = 0;
for(int i = 0; i < n; i++) {
    if(arr[i] > 0) {
        positiveCount++;
    }
}

// Брой гласни букви
int vowelCount = 0;
for(int i = 0; text[i] != '\0'; i++) {
    if(isVowel(text[i])) {
        vowelCount++;
    }
}
```

---

## **4. PATTERN: СУМИРАНЕ С УСЛОВИЕ**

### Сума на елементи по условие

```cpp
int sum = 0;
for(int i = 0; i < n; i++) {
    if(/* условие */) {
        sum += arr[i];
    }
}
```

**Примери:**

```cpp
// Сума на положителни числа
int positiveSum = 0;
for(int i = 0; i < n; i++) {
    if(arr[i] > 0) {
        positiveSum += arr[i];
    }
}

// Сума на отрицателни числа
int negativeSum = 0;
for(int i = 0; i < n; i++) {
    if(arr[i] < 0) {
        negativeSum += arr[i];
    }
}

// Абсолютна стойност на сума
int absSum = (sum < 0) ? -sum : sum;
```

---

## **5. PATTERN: ФЛАГОВЕ (bool променливи)**

### Използване на флагове за проследяване на състояние

```cpp
bool flag = false;  // Началното състояние

for(/* обхождане */) {
    if(/* условие за промяна */) {
        flag = true;  // Променяме състоянието
    }
}

// Проверка след цикъла
if(flag) {
    // Действие
}
```

**⚠️ Трик:** Флаговете са перфектни за "дали има поне един елемент с условие X"

**Примери:**

```cpp
// Дали има отрицателно число
bool hasNegative = false;
for(int i = 0; i < n; i++) {
    if(arr[i] < 0) {
        hasNegative = true;
        break;  // Може да спрем веднага
    }
}

// Дали всички са четни
bool allEven = true;
for(int i = 0; i < n; i++) {
    if(arr[i] % 2 != 0) {
        allEven = false;
        break;
    }
}
```

---

## **6. PATTERN: БРОЕНЕ НА ДУМИ/ЧИСЛА В ТЕКСТ**

### Ключова идея: Флаг "вътре/вън"

```cpp
int count = 0;
bool inWord = false;  // Флаг - вътре в дума ли сме?

for(int i = 0; text[i] != '\0'; i++) {
    if(/* символ от думата */) {
        if(!inWord) {  // Ако не сме били вътре
            count++;   // Нова дума!
            inWord = true;
        }
    } else {  // Разделител
        inWord = false;
    }
}
```

**Примери:**

```cpp
// Брой думи (букви разделени с интервали)
int wordCount = 0;
bool inWord = false;

for(int i = 0; text[i] != '\0'; i++) {
    if(text[i] != ' ') {  // Не е интервал
        if(!inWord) {
            wordCount++;
            inWord = true;
        }
    } else {
        inWord = false;
    }
}

// Брой числа (цифри разделени с други символи)
int numberCount = 0;
bool inNumber = false;

for(int i = 0; text[i] != '\0'; i++) {
    if(text[i] >= '0' && text[i] <= '9') {
        if(!inNumber) {
            numberCount++;
            inNumber = true;
        }
    } else {
        inNumber = false;
    }
}
```

**💡 Ключови моменти:**

1. Флагът показва дали сме "вътре" в последователност
2. Броим само при **начало** на нова последователност
3. Рестартираме флага при разделител

---

## **7. PATTERN: ПРОВЕРКА ЗА НАРАСТВАНЕ/НАМАЛЯВАНЕ**

### Сравняване на съседни елементи

```cpp
bool isIncreasing = true;

for(int i = 0; i < n - 1; i++) {
    if(arr[i] >= arr[i + 1]) {  // НЕ е строго нарастващ
        isIncreasing = false;
        break;
    }
}
```

**Вариации:**

```cpp
// Строго нарастващ (всеки следващ е по-голям)
for(int i = 0; i < n - 1; i++) {
    if(arr[i] >= arr[i + 1]) {
        isIncreasing = false;
        break;
    }
}

// Нарастващ или равен
for(int i = 0; i < n - 1; i++) {
    if(arr[i] > arr[i + 1]) {
        isIncreasing = false;
        break;
    }
}

// Строго намаляващ
for(int i = 0; i < n - 1; i++) {
    if(arr[i] <= arr[i + 1]) {
        isDecreasing = false;
        break;
    }
}
```

**⚠️ Внимание:**

- `arr[i] >= arr[i+1]` проверява дали **НЕ е** строго нарастващ
- `arr[i] > arr[i+1]` проверява дали **НЕ е** нарастващ или равен

---

## **8. PATTERN: АРИТМЕТИЧНА ПРОГРЕСИЯ**

### Проверка дали редица е аритметична прогресия

**Идея:** Разликата между всеки два съседни елемента трябва да е константна

```cpp
bool isArithmetic = true;

if(n > 1) {
    int diff = arr[1] - arr[0];  // Изчисляваме разликата

    for(int i = 1; i < n - 1; i++) {
        if(arr[i + 1] - arr[i] != diff) {  // Проверяваме
            isArithmetic = false;
            break;
        }
    }
}
```

**Примери:**

- `[2, 4, 6, 8]` → diff = 2 → аритметична ✅
- `[5, 5, 5, 5]` → diff = 0 → аритметична ✅
- `[1, 3, 7, 9]` → diff не е константна → НЕ е аритметична ❌

---

## **9. PATTERN: НАЙ-ДЪЛГА ПОСЛЕДОВАТЕЛНОСТ**

### Намиране на най-дълга последователност от еднакви елементи

```cpp
int maxLen = 1;
int currentLen = 1;

for(int i = 1; i < n; i++) {  // Започваме от 1!
    if(arr[i] == arr[i - 1]) {  // Същият елемент
        currentLen++;
        if(currentLen > maxLen) {
            maxLen = currentLen;
        }
    } else {  // Различен елемент
        currentLen = 1;  // Рестартираме
    }
}
```

**Ключови моменти:**

- `maxLen` пази най-дългата намерена до момента
- `currentLen` пази текущата последователност
- Започваме от индекс 1 (сравняваме с предишния)
- При промяна рестартираме текущата дължина

**Примери:**

- `"aaabbbcc"` → maxLen = 3
- `"aabbbbaa"` → maxLen = 4

---

## **10. МАТРИЦИ (2D масиви)**

### Деклариране и въвеждане

```cpp
int n;
std::cin >> n;

int matrix[20][20];  // Фиксиран размер

// Въвеждане по редове
for(int i = 0; i < n; i++) {
    for(int j = 0; j < n; j++) {
        std::cin >> matrix[i][j];
    }
}
```

### Обхождане на матрица

```cpp
// По редове (отгоре надолу, отляво надясно)
for(int i = 0; i < n; i++) {
    for(int j = 0; j < n; j++) {
        // matrix[i][j]
    }
}

// По колони (отляво надясно, отгоре надолу)
for(int j = 0; j < n; j++) {
    for(int i = 0; i < n; i++) {
        // matrix[i][j]
    }
}

// Главен диагонал (i == j)
for(int i = 0; i < n; i++) {
    // matrix[i][i]
}

// Под главния диагонал (i > j)
for(int i = 0; i < n; i++) {
    for(int j = 0; j < i; j++) {
        // matrix[i][j]
    }
}

// Над главния диагонал (i < j)
for(int i = 0; i < n; i++) {
    for(int j = i + 1; j < n; j++) {
        // matrix[i][j]
    }
}
```

### Проверка за симметрична матрица

**Идея:** Елементът на `[i][j]` трябва да е равен на `[j][i]`

```cpp
bool symmetric = true;

for(int i = 0; i < n; i++) {
    for(int j = 0; j < n; j++) {
        if(matrix[i][j] != matrix[j][i]) {
            symmetric = false;
            break;
        }
    }
    if(!symmetric) break;  // Излизаме и от външния цикъл
}
```

**Оптимизация:** Проверявайте само над диагонала

```cpp
bool symmetric = true;

for(int i = 0; i < n; i++) {
    for(int j = i + 1; j < n; j++) {  // Само над диагонала
        if(matrix[i][j] != matrix[j][i]) {
            symmetric = false;
            break;
        }
    }
    if(!symmetric) break;
}
```

---

## **11. PATTERN: ОБРАБОТКА ПО РЕДОВЕ/КОЛОНИ**

### Проверка на свойство за всеки ред

```cpp
int count = 0;  // Брой редове със свойството

for(int i = 0; i < n; i++) {
    bool hasProperty = true;  // Флаг за текущия ред

    for(int j = 0; j < n; j++) {
        if(/* проверка */) {
            hasProperty = false;
            break;
        }
    }

    if(hasProperty) {
        count++;
    }
}
```

**Примери:**

```cpp
// Брой редове с намаляващи елементи
int decreasingRows = 0;

for(int i = 0; i < n; i++) {
    bool isDecreasing = true;

    for(int j = 0; j < n - 1; j++) {
        if(matrix[i][j] <= matrix[i][j + 1]) {
            isDecreasing = false;
            break;
        }
    }

    if(isDecreasing) {
        decreasingRows++;
    }
}
```

---

## **12. PATTERN: БЕЗ МАСИВИ (On-the-fly обработка)**

### Когато НЕ е нужно да запазваме данните

**Идея:** Обработвайте докато четете

```cpp
int n;
std::cin >> n;

// Без масив!
for(int i = 0; i < n; i++) {
    int prev, current;
    std::cin >> prev;

    bool hasProperty = true;

    for(int j = 1; j < n; j++) {
        std::cin >> current;

        // Обработка
        if(prev <= current) {
            hasProperty = false;
        }

        prev = current;  // Запомняме за следващата итерация
    }

    if(hasProperty) {
        // Действие
    }
}
```

**⚠️ Кога е полезно:**

- Когато обработваме ред по ред
- Когато проверяваме само съседни елементи
- Когато искаме да спестим памет

---

## **13. PATTERN: СРАВНЕНИЕ НА ЧАСТИ**

### Сравняване на две половини на масив

```cpp
int half = n / 2;
bool identical = true;

for(int i = 0; i < half; i++) {
    if(arr[i] != arr[half + i]) {
        identical = false;
        break;
    }
}
```

**Примери:**

```cpp
// Огледални половини
// [1, 2, 3, 1, 2, 3] → true
int half = n / 2;
bool mirror = true;

for(int i = 0; i < half; i++) {
    if(arr[i] != arr[half + i]) {
        mirror = false;
        break;
    }
}

// Палиндром (обърнати половини)
// [1, 2, 3, 3, 2, 1] → true
bool palindrome = true;

for(int i = 0; i < n / 2; i++) {
    if(arr[i] != arr[n - 1 - i]) {
        palindrome = false;
        break;
    }
}
```

---

## **14. PATTERN: СЛЕПВАНЕ НА МАСИВИ**

### Редуване на елементи от два масива

```cpp
int merged[50];
int idx = 0;
int maxLen = (n > m) ? n : m;  // Максималната дължина

for(int i = 0; i < maxLen; i++) {
    if(i < n) {
        merged[idx++] = arr1[i];
    }
    if(i < m) {
        merged[idx++] = arr2[i];
    }
}

// idx сега е дължината на merged
```

**Резултат:** `[a1, b1, a2, b2, a3, b3, ...]`

---

## **15. PATTERN: СЪСТОЯНИЕ В ТЕКСТ**

### Проследяване на начало на изречение

```cpp
int count = 0;
bool startOfSentence = true;  // В началото сме

for(int i = 0; text[i] != '\0'; i++) {
    if(startOfSentence) {
        // Проверка само в началото на изречение
        if(text[i] >= 'A' && text[i] <= 'Z') {
            count++;
        }
        startOfSentence = false;  // Вече не сме в началото
    }

    // Край на изречение
    if(text[i] == '.' || text[i] == '!' || text[i] == '?') {
        startOfSentence = true;
    } else if(text[i] != ' ') {
        startOfSentence = false;
    }
}
```

**Ключови моменти:**

- Флагът показва дали сме в началото на изречение
- Проверяваме само когато флагът е `true`
- Рестартираме след препинателен знак

---

## **16. ТРИКОВЕ И ЧЕСТО СРЕЩАНИ ГРЕШКИ**

### ✅ Добри практики

```cpp
// 1. Инициализирайте променливи
int count = 0;  // НЕ: int count;

// 2. Проверявайте границите
for(int i = 0; i < n - 1; i++) {  // НЕ: i < n при arr[i+1]
    // ...
}

// 3. Използвайте break за ранно излизане
for(int i = 0; i < n; i++) {
    if(arr[i] == target) {
        found = true;
        break;  // Не продължаваме излишно
    }
}

// 4. Внимавайте с терминиращата нула
for(int i = 0; text[i] != '\0'; i++) {  // НЕ: i < n без да знаем n
    // ...
}
```

### ❌ Често срещани грешки

```cpp
// ГРЕШКА 1: Индекс извън границите
for(int i = 0; i < n; i++) {
    if(arr[i] == arr[i + 1]) {  // ❌ При i=n-1 -> arr[n]
        // ...
    }
}
// ПРАВИЛНО:
for(int i = 0; i < n - 1; i++) {  // ✅

// ГРЕШКА 2: Забравена инициализация
int sum;  // ❌ Съдържа random стойност
sum += arr[i];

// ПРАВИЛНО:
int sum = 0;  // ✅

// ГРЕШКА 3: Сравняване вместо присвояване
if(arr[i] = 5) {  // ❌ Присвоява 5, винаги true!
// ПРАВИЛНО:
if(arr[i] == 5) {  // ✅

// ГРЕШКА 4: Четене на низ с интервали
std::cin >> text;  // ❌ Чете до първия интервал
// ПРАВИЛНО:
std::cin.getline(text, 100);  // ✅
```

---

## **17. TEMPLATE ЗА ОСНОВНИ ЗАДАЧИ**

### Масив + обработка

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int arr[100];
    for(int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    // ОБРАБОТКА ТУК

    // ИЗХОД

    return 0;
}
```

### Символен низ + обработка

```cpp
#include <iostream>

int main() {
    char text[256];
    std::cin.getline(text, 256);

    // ОБРАБОТКА ТУК

    // ИЗХОД

    return 0;
}
```

### Матрица + обработка

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int matrix[20][20];
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            std::cin >> matrix[i][j];
        }
    }

    // ОБРАБОТКА ТУК

    // ИЗХОД

    return 0;
}
```

---

## **18. CHECKLIST ПРЕДИ РЕШАВАНЕ**

### Прочетете условието внимателно:

- [ ] Какъв е входът? (масив, низ, матрица?)
- [ ] Какъв е изходът? (число, текст, да/не?)
- [ ] Има ли специални случаи? (празен масив, само един елемент?)
- [ ] Какви са ограниченията? (размер на масива, стойности?)

### Планирайте решението:

- [ ] Трябва ли да запазя всички данни? (масив vs on-the-fly)
- [ ] Какви променливи ми трябват? (брояч, сума, флаг?)
- [ ] Какъв цикъл/цикли ще използвам?
- [ ] Има ли готов pattern който мога да приложа?

### Напишете кода:

- [ ] Инициализирайте всички променливи
- [ ] Внимавайте с границите на циклите
- [ ] Използвайте `break` където е подходящо
- [ ] Проверете специалните случаи

### Тествайте:

- [ ] Работи ли с примерите от условието?
- [ ] Какво става с граничните случаи? (n=1, празен низ?)
- [ ] Има ли индекс извън границите?

---

## **19. БЪРЗИ REFERENCE ФОРМУЛИ**

```cpp
// Абсолютна стойност
int abs = (n < 0) ? -n : n;

// Максимум от две числа
int max = (a > b) ? a : b;

// Минимум от две числа
int min = (a < b) ? a : b;

// Дали число е четно
bool isEven = (n % 2 == 0);

// Дали число е нечетно
bool isOdd = (n % 2 != 0);

// Размяна на две числа
int temp = a;
a = b;
b = temp;

// Обръщане на условие
if(!condition) {  // Ако НЕ е изпълнено
    // ...
}
```

---

## **20. ФИНАЛНИ СЪВЕТИ ЗА ТЕСТА**

### Време и стратегия:

1. **Прочетете всички задачи** в началото
2. **Започнете с лесните** - набирайте точки бързо
3. **Не се блокирайте** на една задача - преминете напред
4. **Оставете време** за проверка в края

### По време на решаване:

1. **Пишете чист код** - по-лесно за debugging
2. **Използвайте описателни имена** - `count`, `sum`, `isValid`
3. **Коментирайте** сложната логика
4. **Тествайте често** - не чакайте да напишете всичко

### Debugging:

1. **Принтирайте междинни стойности** с `std::cout`
2. **Проверете границите** на циклите
3. **Проверете инициализациите** на променливите
4. **Проверете специалните случаи** (n=0, n=1, празен низ)

---

**Успех! 🚀**

_Този cheatsheet покрива всички patterns и техники необходими за решаване на задачите от практиката._
