# **ТЕОРИЯ - СИМВОЛНИ НИЗОВЕ (char arrays)**

---

# _КАКВО Е НИЗ (char array)?_

## Основни понятия

Символният низ (char array) е **масив от символи (char)**, който завършва със специален терминиращ символ `'\0'` (null terminator). Това е фундаментален начин за работа с текст в C++.

### Защо char arrays, а не string?

В C++ има два основни начина за работа с текст:

- **`char` масиви** (C-style strings) - базов, ниско-ниво начин
- **`std::string`** - по-висококачествен, модерен начин

На този етап се фокусираме върху **char масивите**, защото:

1. ✅ Разбираме как текстът се съхранява в паметта
2. ✅ Учим се да работим директно с паметта
3. ✅ Развиваме алгоритмично мислене
4. ✅ Подготовка за по-сложни структури от данни

---

## 1) Деклариране и инициализация

### Основен синтаксис:

```c++
char <име>[<размер>];
```

### Различни начини за деклариране:

#### 1. Деклариране без инициализация:

```c++
char text[100];  // Масив за до 99 символа + '\0'
```

#### 2. Инициализация със стринг литерал:

```c++
char greeting[] = "Hello";
// Автоматично се добавя '\0'
// В паметта: 'H' 'e' 'l' 'l' 'o' '\0' (6 bytes)
```

#### 3. Инициализация със зададен размер:

```c++
char name[50] = "Alice";
// name[0]='A', name[1]='l', name[2]='i', name[3]='c', name[4]='e', name[5]='\0'
// Останалите 44 клетки са неинициализирани
```

#### 4. Посимволна инициализация:

```c++
char word[] = {'H', 'e', 'l', 'l', 'o', '\0'};
// ⚠️ ВАЖНО: Трябва ръчно да добавим '\0'!
```

#### 5. Грешна инициализация:

```c++
char bad[] = {'H', 'e', 'l', 'l', 'o'};  // ❌ ГРЕШКА! Липсва '\0'
// Функциите за стрингове няма да работят правилно!
```

---

## 2) Терминиращата нула '\0'

### ⚠️ КРИТИЧНО ВАЖНО ПРАВИЛО:

**Всеки валиден символен низ ТРЯБВА да завършва с '\0'!**

### Защо е необходима?

Терминиращата нула показва **края на низа**. Без нея:

- ❌ Функциите не знаят къде свършва низът
- ❌ Четенето продължава в недефинирана памет
- ❌ Програмата може да се счупи (crash) или да покаже боклук

### Как изглежда в паметта?

```c++
char text[] = "FMI";

// В паметта:
// Index:  [0]  [1]  [2]  [3]
// Value:  'F'  'M'  'I' '\0'
```

### Примери:

```c++
char str1[] = "ABC";     // Размер: 4 ('A','B','C','\0')
char str2[10] = "ABC";   // Размер: 10, използвани: 4

// Проверка за край на низ:
for(int i = 0; str1[i] != '\0'; i++) {
    std::cout << str1[i];  // A B C
}
```

### ⚡ Често срещана грешка:

```c++
char wrong[3] = "ABC";  // ❌ ГРЕШКА!
// Няма място за '\0'! (нужни са 4 байта)

char correct[4] = "ABC";  // ✅ Правилно
```

---

## 3) Достъп до елементи

Символните низове са масиви, така че достъпът е същият:

```c++
char word[] = "Hello";

// Четене на елементи:
char first = word[0];   // 'H'
char second = word[1];  // 'e'
char last = word[4];    // 'o'
char null = word[5];    // '\0'

// Промяна на елементи:
word[0] = 'M';
word[1] = 'e';
// Сега: "Mello"

// ⚠️ НЕ забравяйте '\0' при ръчна промяна!
word[5] = '\0';  // Терминираща нула на правилно място
```

---

## 4) Въвеждане на символни низове

### Метод 1: `std::cin >>` (за една дума без интервали)

```c++
char word[100];
std::cin >> word;  // Чете до интервал, нов ред или табулация

// Вход: "Hello World"
// word съдържа: "Hello" (спира на интервала)
```

**Характеристики:**

- ✅ Автоматично добавя '\0'
- ✅ Прескача водещи whitespace символи
- ❌ Чете само първата дума

### Метод 2: `std::cin.getline()` (за цял ред с интервали)

```c++
char line[256];
std::cin.getline(line, 256);  // Чете до 255 символа + '\0'

// Вход: "Hello World"
// line съдържа: "Hello World"
```

**Характеристики:**

- ✅ Чете цял ред (включително интервали)
- ✅ Автоматично добавя '\0'
- ✅ Спира при нов ред '\n'
- ⚠️ Трябва да зададем максимален размер

### Метод 3: `std::cin.get()` (посимволно четене)

```c++
char ch;
int i = 0;
char text[100];

while(std::cin.get(ch) && ch != '\n') {
    text[i++] = ch;
}
text[i] = '\0';  // ⚠️ Не забравяйте терминиращата нула!
```

### ⚡ Важно за getline след >>:

```c++
int n;
char text[100];

std::cin >> n;           // Чете число, оставя '\n' в буфера
std::cin.ignore();       // ⚠️ ВАЖНО! Премахва '\n' от буфера
std::cin.getline(text, 100);  // Сега работи правилно
```

---

## 5) Извеждане на символни низове

### Метод 1: Директно извеждане

```c++
char text[] = "Hello, FMI!";
std::cout << text;  // Извежда целия низ до '\0'
```

### Метод 2: Посимволно извеждане

```c++
char text[] = "Hello";

// Вариант 1: Използване на '\0'
for(int i = 0; text[i] != '\0'; i++) {
    std::cout << text[i];
}

// Вариант 2: Използване на strlen (вижте по-долу)
for(int i = 0; i < strlen(text); i++) {
    std::cout << text[i];
}
```

### Метод 3: На нов ред всеки символ

```c++
char text[] = "FMI";
for(int i = 0; text[i] != '\0'; i++) {
    std::cout << text[i] << std::endl;
}
// Изход:
// F
// M
// I
```

---

## 6) Стандартни библиотечни функции (`<cstring>`)

За да използвате тези функции, включете:

```c++
#include <cstring>
```

### 🔹 `strlen()` - Дължина на низ

Връща броя на символите **БЕЗ** терминиращата нула.

```c++
char text[] = "Hello";
int len = strlen(text);  // len = 5 (не брои '\0')

// Ръчна имплементация:
int myStrlen(const char* str) {
    int count = 0;
    while(str[count] != '\0') {
        count++;
    }
    return count;
}
```

**⚠️ Важно:**

- `strlen("ABC")` → 3
- Реален размер в паметта: 4 байта ('A','B','C','\0')

### 🔹 `strcpy()` - Копиране на низ

Копира символи от `source` в `destination` (включително '\0').

```c++
char source[] = "Hello";
char dest[20];

strcpy(dest, source);  // dest става "Hello"

// ⚠️ ВНИМАНИЕ: dest трябва да е достатъчно голям!
char small[3];
strcpy(small, source);  // ❌ ГРЕШКА! Buffer overflow!
```

**Ръчна имплементация:**

```c++
void myStrcpy(char* dest, const char* src) {
    int i = 0;
    while(src[i] != '\0') {
        dest[i] = src[i];
        i++;
    }
    dest[i] = '\0';  // Не забравяйте!
}
```

### 🔹 `strcat()` - Конкатенация (залепване)

Добавя `source` в края на `destination`.

```c++
char dest[50] = "Hello";
char src[] = " World";

strcat(dest, src);  // dest става "Hello World"

std::cout << dest;  // "Hello World"
```

**⚠️ Важно:**

- `dest` трябва да е достатъчно голям за двата низа + '\0'
- Търси '\0' в dest и започва копиране от там

**Ръчна имплементация:**

```c++
void myStrcat(char* dest, const char* src) {
    // Намираме края на dest
    int i = 0;
    while(dest[i] != '\0') {
        i++;
    }

    // Копираме src след края на dest
    int j = 0;
    while(src[j] != '\0') {
        dest[i] = src[j];
        i++;
        j++;
    }
    dest[i] = '\0';
}
```

### 🔹 `strcmp()` - Сравняване на низове

Сравнява два низа **лексикографски** (по азбучен ред в ASCII).

```c++
char str1[] = "Apple";
char str2[] = "Banana";
char str3[] = "Apple";

int result1 = strcmp(str1, str2);  // < 0 (str1 е "по-малък")
int result2 = strcmp(str2, str1);  // > 0 (str2 е "по-голям")
int result3 = strcmp(str1, str3);  // == 0 (равни са)
```

**Връщани стойности:**

- `< 0` ако първият низ е "по-малък"
- `== 0` ако низовете са идентични
- `> 0` ако първият низ е "по-голям"

**Ръчна имплементация:**

```c++
int myStrcmp(const char* str1, const char* str2) {
    int i = 0;
    while(str1[i] != '\0' && str2[i] != '\0') {
        if(str1[i] != str2[i]) {
            return str1[i] - str2[i];
        }
        i++;
    }
    return str1[i] - str2[i];  // Проверка за различна дължина
}
```

### 🔹 `strchr()` - Търсене на символ

Намира първото срещане на символ в низ.

```c++
char text[] = "Hello World";
char* ptr = strchr(text, 'o');  // Връща указател към първо 'o'

if(ptr != nullptr) {
    std::cout << "Found at position: " << (ptr - text);  // 4
}
```

**Пример - Намиране на второто срещане:**

```c++
#include <iostream>
#include <cstring>

int main() {
    char text[] = "Hello World";
    char* ptr1 = strchr(text, 'o');  // Намира първо 'o' на позиция 4
    char* ptr2 = nullptr;

    if(ptr1 != nullptr) {
        ptr2 = strchr(ptr1 + 1, 'o');  // Търси след първото 'o'
    }

    if(ptr2 != nullptr) {
        std::cout << "Second 'o' at position: " << (ptr2 - text);  // 7
    }

    return 0;
}
```

### 🔹 `strstr()` - Търсене на подниз

Намира първото срещане на подниз.

```c++
char text[] = "Hello World";
char* ptr = strstr(text, "World");

if(ptr != nullptr) {
    std::cout << "Found: " << ptr;  // "World"
}
```

**Пример - Намиране на множество срещания:**

```c++
#include <iostream>
#include <cstring>

int main() {
    char myStr[] = "The rain in Spain falls mainly on the plains";
    char* myPtr = strstr(myStr, "ain");
    char* myPtr2 = nullptr;

    if(myPtr != nullptr) {
        std::cout << "First occurrence: " << myPtr << std::endl;
        // Изход: "ain in Spain falls mainly on the plains"

        myPtr2 = strstr(myPtr + 1, "ain");  // Започваме търсенето след първото "ain"
    }

    if(myPtr2 != nullptr) {
        std::cout << "Second occurrence: " << myPtr2 << std::endl;
        // Изход: "ain falls mainly on the plains"
    }

    return 0;
}
```

**Намиране на всички срещания:**

```c++
char text[] = "abc abc abc";
char* ptr = text;

std::cout << "All occurrences of 'abc':" << std::endl;
while((ptr = strstr(ptr, "abc")) != nullptr) {
    std::cout << "Position: " << (ptr - text) << std::endl;
    ptr++;  // Преместваме се с 1 позиция напред
}
// Изход: Position: 0, Position: 4, Position: 8
```

---

## 7) Обхождане на символен низ

### Метод 1: Използване на '\0'

```c++
char text[] = "FMI";

for(int i = 0; text[i] != '\0'; i++) {
    std::cout << text[i] << " ";
}
// Изход: F M I
```

### Метод 2: Използване на strlen()

```c++
char text[] = "FMI";
int len = strlen(text);

for(int i = 0; i < len; i++) {
    std::cout << text[i] << " ";
}
```

**⚠️ Оптимизация:** Не викайте `strlen()` във всяка итерация!

```c++
// ❌ НЕЕФЕКТИВНО:
for(int i = 0; i < strlen(text); i++) { ... }
// strlen() се вика n пъти → O(n²)

// ✅ ЕФЕКТИВНО:
int len = strlen(text);
for(int i = 0; i < len; i++) { ... }
// strlen() се вика веднъж → O(n)
```

### Метод 3: Обхождане назад

```c++
char text[] = "FMI";
int len = strlen(text);

for(int i = len - 1; i >= 0; i--) {
    std::cout << text[i];
}
// Изход: IMF
```

---

## 8) Работа със символи - Полезни проверки

### ASCII таблица (важни кодове):

![ASCII Table](ASCII-Table.png)

**Най-важни ASCII кодове за запомняне:**

```
Символ    ASCII код    Обяснение
'0'-'9'   48-57        Цифри
'A'-'Z'   65-90        Главни букви (латиница)
'a'-'z'   97-122       Малки букви (латиница)
' '       32           Интервал (space)
'\n'      10           Нов ред (newline)
'\t'      9            Табулация (tab)
'\0'      0            Терминираща нула (null terminator)
```

**Полезни факти:**

- Разликата между малка и главна буква е точно 32 (`'a' - 'A' = 32`)
- Цифрите започват от ASCII код 48 (`'0' = 48`, `'1' = 49`, ..., `'9' = 57`)
- За да превърнете цифра-символ в число: `int num = ch - '0';`
- За да превърнете число в цифра-символ: `char ch = num + '0';`

### Проверки за тип символ:

```c++
char c = 'A';

// Дали е главна буква
bool isUpper = (c >= 'A' && c <= 'Z');

// Дали е малка буква
bool isLower = (c >= 'a' && c <= 'z');

// Дали е буква (латиница)
bool isAlpha = (c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z');

// Дали е цифра
bool isDigit = (c >= '0' && c <= '9');

// Дали е буква или цифра
bool isAlphanumeric = isAlpha || isDigit;

// Дали е whitespace (интервал, табулация, нов ред)
bool isWhitespace = (c == ' ' || c == '\t' || c == '\n' || c == '\r');

// Дали е гласна буква
bool isVowel = (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' ||
                c == 'A' || c == 'E' || c == 'I' || c == 'O' || c == 'U');

// Дали е съгласна буква
bool isConsonant = isAlpha && !isVowel;
```

### Трансформации на символи:

```c++
char c = 'a';

// Главна буква → малка буква
if(c >= 'A' && c <= 'Z') {
    c = c + ('a' - 'A');  // или c = c + 32
}

// Малка буква → главна буква
if(c >= 'a' && c <= 'z') {
    c = c - ('a' - 'A');  // или c = c - 32
}

// Toggle (обръщане)
if(c >= 'A' && c <= 'Z') {
    c = c + 32;
} else if(c >= 'a' && c <= 'z') {
    c = c - 32;
}
```

**Функции за трансформация:**

```c++
char toUpper(char c) {
    if(c >= 'a' && c <= 'z') {
        return c - 32;
    }
    return c;
}

char toLower(char c) {
    if(c >= 'A' && c <= 'Z') {
        return c + 32;
    }
    return c;
}
```

---

## 9) Често срещани операции с низове

### 🔸 Дължина на низ (ръчна имплементация)

```c++
int stringLength(const char* str) {
    int len = 0;
    while(str[len] != '\0') {
        len++;
    }
    return len;
}
```

### 🔸 Копиране на низ

```c++
void copyString(char* dest, const char* src) {
    int i = 0;
    while(src[i] != '\0') {
        dest[i] = src[i];
        i++;
    }
    dest[i] = '\0';
}
```

### 🔸 Обръщане на низ

```c++
void reverseString(char* str) {
    int len = strlen(str);
    for(int i = 0; i < len / 2; i++) {
        // Swap str[i] и str[len - 1 - i]
        char temp = str[i];
        str[i] = str[len - 1 - i];
        str[len - 1 - i] = temp;
    }
}

// Примери:
char text[] = "Hello";
reverseString(text);
// text = "olleH"
```

### 🔸 Превръщане на низ в главни букви

```c++
void toUpperCase(char* str) {
    for(int i = 0; str[i] != '\0'; i++) {
        if(str[i] >= 'a' && str[i] <= 'z') {
            str[i] = str[i] - 32;
        }
    }
}

// Примери:
char text[] = "Hello World";
toUpperCase(text);
// text = "HELLO WORLD"
```

### 🔸 Превръщане на низ в малки букви

```c++
void toLowerCase(char* str) {
    for(int i = 0; str[i] != '\0'; i++) {
        if(str[i] >= 'A' && str[i] <= 'Z') {
            str[i] = str[i] + 32;
        }
    }
}
```

### 🔸 Проверка дали символ се съдържа в низ

```c++
bool contains(const char* str, char ch) {
    for(int i = 0; str[i] != '\0'; i++) {
        if(str[i] == ch) {
            return true;
        }
    }
    return false;
}

// Пример:
contains("ABCD", 'C');  // true
contains("ABCD", 'c');  // false
```

### 🔸 Броене на срещания на символ

```c++
int countOccurrences(const char* str, char ch) {
    int count = 0;
    for(int i = 0; str[i] != '\0'; i++) {
        if(str[i] == ch) {
            count++;
        }
    }
    return count;
}

// Пример:
countOccurrences("Mississippi", 's');  // 4
```

### 🔸 Проверка за палиндром

```c++
bool isPalindrome(const char* str) {
    int len = strlen(str);
    for(int i = 0; i < len / 2; i++) {
        if(str[i] != str[len - 1 - i]) {
            return false;
        }
    }
    return true;
}

// Примери:
isPalindrome("racecar");  // true
isPalindrome("hello");    // false
isPalindrome("A");        // true
```

### 🔸 Премахване на интервали

```c++
void removeSpaces(char* str) {
    int i = 0;  // За четене
    int j = 0;  // За писане

    while(str[i] != '\0') {
        if(str[i] != ' ') {
            str[j] = str[i];
            j++;
        }
        i++;
    }
    str[j] = '\0';
}

// Пример:
char text[] = "Hello World";
removeSpaces(text);
// text = "HelloWorld"
```

---

## 10) Масив от низове (2D char array)

### Деклариране:

```c++
// Масив от 10 низа, всеки до 50 символа
char names[10][50];
```

### Инициализация:

```c++
char days[7][10] = {
    "Monday",
    "Tuesday",
    "Wednesday",
    "Thursday",
    "Friday",
    "Saturday",
    "Sunday"
};
```

### Въвеждане и извеждане:

```c++
char students[5][50];
int n = 5;

// Въвеждане
for(int i = 0; i < n; i++) {
    std::cout << "Enter name " << i+1 << ": ";
    std::cin.getline(students[i], 50);
}

// Извеждане
for(int i = 0; i < n; i++) {
    std::cout << students[i] << std::endl;
}
```

---

## 11) Често срещани грешки и как да ги избегнем

### ❌ Грешка 1: Липсваща терминираща нула

```c++
// ГРЕШНО:
char str[5] = {'H', 'e', 'l', 'l', 'o'};
std::cout << str;  // Недефинирано поведение!

// ПРАВИЛНО:
char str[6] = {'H', 'e', 'l', 'l', 'o', '\0'};
// или
char str[] = "Hello";
```

### ❌ Грешка 2: Buffer overflow

```c++
// ГРЕШНО:
char small[5];
strcpy(small, "Hello World");  // Препълване!

// ПРАВИЛНО:
char big[50];
strcpy(big, "Hello World");
```

### ❌ Грешка 3: Неинициализиран масив

```c++
// ГРЕШНО:
char text[100];
std::cout << text;  // Може да покаже боклук!

// ПРАВИЛНО:
char text[100] = "";  // Празен низ
// или
char text[100];
text[0] = '\0';
```

### ❌ Грешка 4: Сравняване с ==

```c++
char str1[] = "Hello";
char str2[] = "Hello";

// ГРЕШНО:
if(str1 == str2) { ... }  // Сравнява адреси, не съдържание!

// ПРАВИЛНО:
if(strcmp(str1, str2) == 0) { ... }
```

### ❌ Грешка 5: Присвояване с =

```c++
char dest[50];
char src[] = "Hello";

// ГРЕШНО:
dest = src;  // ❌ Не можете да присвоите масиви!

// ПРАВИЛНО:
strcpy(dest, src);
```

---

## 12) Памет и ефективност

### Как се съхраняват низовете?

```c++
char text[] = "FMI";

// В паметта (например адрес 1000):
// Адрес:  1000  1001  1002  1003
// Стойност: 'F'   'M'   'I'  '\0'
```

### Размер в паметта:

```c++
char text1[] = "Hello";      // 6 bytes (5 символа + '\0')
char text2[100] = "Hello";   // 100 bytes (заделени, използвани 6)

sizeof(text1);  // 6
sizeof(text2);  // 100
strlen(text1);  // 5
strlen(text2);  // 5
```

### ⚡ Производителност:

- `strlen()` е O(n) - обхожда целия низ!
- Ако викате `strlen()` в цикъл → O(n²)
- **Решение:** Изчислете дължината веднъж преди цикъла

```c++
// ❌ БАВНО - O(n²):
for(int i = 0; i < strlen(text); i++) { ... }

// ✅ БЪРЗО - O(n):
int len = strlen(text);
for(int i = 0; i < len; i++) { ... }
```

---

## 13) Практични съвети

### ✅ DO (правете):

1. **Винаги** оставяйте място за '\0' при деклариране
2. **Проверявайте** размера преди копиране/залепване
3. **Инициализирайте** масивите преди употреба
4. **Използвайте** `strcmp()` за сравнение, не `==`
5. **Пазете** дължината в променлива, ако я използвате често

### ❌ DON'T (избягвайте):

1. **Не** забравяйте терминиращата нула
2. **Не** използвайте `=` за присвояване на низове
3. **Не** сравнявайте с `==`
4. **Не** викайте `strlen()` многократно без нужда
5. **Не** достъпвайте памет извън границите на масива

---

## 🎯 Обобщение

### Ключови точки:

1. Символният низ е **масив от char** + '\0'
2. '\0' е **задължителен** за края на низа
3. Индексите са от **0 до strlen(str)-1**
4. Използвайте **`<cstring>`** функции за стандартни операции
5. **Внимавайте** за размера на масива при копиране/залепване

### Често използвани функции:

| Функция             | Действие       | Пример                 |
| ------------------- | -------------- | ---------------------- |
| `strlen(str)`       | Дължина на низ | `strlen("ABC")` → 3    |
| `strcpy(dest, src)` | Копиране       | `strcpy(a, "Hi")`      |
| `strcat(dest, src)` | Залепване      | `strcat(a, "!")`       |
| `strcmp(s1, s2)`    | Сравняване     | `strcmp("A", "B")` < 0 |
| `strchr(str, ch)`   | Търси символ   | `strchr("ABC", 'B')`   |

---

**Успех с упражненията! 🚀**
