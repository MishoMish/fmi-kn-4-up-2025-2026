# **ПОДГОТОВКА ЗА ТЕСТ - СЕДМИЦА 6**

## **Упражнения с Масиви и Символни Низове**

---

**ВАЖНО:** Всички задачи използват само статична памет и масиви (без динамична алокация).

**💡 Съвет:** Опитайте се да решите задачите самостоятелно преди да разгледате решенията!

**📖 Как да използвам решенията:** Кликнете на "📝 Покажи решение" за да разгънете/свиете кода.

---

## **ЗАДАЧА 1: Броене на думи в текст**

От стандартния вход се въвежда символен низ с не повече от 200 символа, съдържащ букви и интервали. Напишете програма, която извежда броя на думите в текста.

**Дума** е всяка последователност от букви, разделена от интервали или краищата на низа.

**Примери:**

```
Input: "Hello world"
Output: 2

Input: "  C plus   plus  "
Output: 3

Input: "Programming"
Output: 1
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    char text[201];
    std::cin.getline(text, 201);

    int wordCount = 0;
    bool inWord = false;

    for(int i = 0; text[i] != '\0'; i++) {
        if(text[i] != ' ') {
            if(!inWord) {
                wordCount++;
                inWord = true;
            }
        } else {
            inWord = false;
        }
    }

    std::cout << wordCount << std::endl;
    return 0;
}
```

</details>

---

## **ЗАДАЧА 2: Сумиране на положителни и отрицателни числа**

От стандартния вход се въвежда число `n` (n ≤ 100), последвано от n цели числа. Напишете програма, която изчислява сумата на всички положителни числа и сумата на всички отрицателни числа, след което ги извежда на два реда.

**Примери:**

```
Input:
5
3 -2 7 -1 5

Output:
Sum of positive: 15
Sum of negative: -3
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int arr[100];
    for(int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    int positiveSum = 0;
    int negativeSum = 0;

    for(int i = 0; i < n; i++) {
        if(arr[i] > 0) {
            positiveSum += arr[i];
        } else if(arr[i] < 0) {
            negativeSum += arr[i];
        }
    }

    std::cout << "Sum of positive: " << positiveSum << std::endl;
    std::cout << "Sum of negative: " << negativeSum << std::endl;

    return 0;
}
```

</details>

---

## **ЗАДАЧА 3: Преброяване на гласни и съгласни**

От стандартния вход се въвежда символен низ с не повече от 250 латински букви (малки и главни). Напишете програма, която извежда броя на гласните букви и броя на съгласните букви.

**Гласни:** a, e, i, o, u, A, E, I, O, U

**Примери:**

```
Input: "Hello World"
Output:
Vowels: 3
Consonants: 7
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

bool isVowel(char c) {
    return c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' ||
           c == 'A' || c == 'E' || c == 'I' || c == 'O' || c == 'U';
}

int main() {
    char text[251];
    std::cin.getline(text, 251);

    int vowels = 0;
    int consonants = 0;

    for(int i = 0; text[i] != '\0'; i++) {
        if((text[i] >= 'a' && text[i] <= 'z') ||
           (text[i] >= 'A' && text[i] <= 'Z')) {
            if(isVowel(text[i])) {
                vowels++;
            } else {
                consonants++;
            }
        }
    }

    std::cout << "Vowels: " << vowels << std::endl;
    std::cout << "Consonants: " << consonants << std::endl;

    return 0;
}
```

</details>

---

## **ЗАДАЧА 4: Броене на въпросителни изречения**

От стандартния вход се въвежда символен низ с не повече от 254 символа, съдържащ латински букви, интервали и препинателните знаци '.', '!' и '?'. Напишете програма, която извежда броя на въпросителните изречения в текста.

**Въпросително изречение** е всяко изречение, което завършва с '?'.

**Примери:**

```
Input: "How are you? I am fine. What about you?"
Output: 2

Input: "Hello! Good morning!"
Output: 0
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    char text[255];
    std::cin.getline(text, 255);

    int questionCount = 0;

    for(int i = 0; text[i] != '\0'; i++) {
        if(text[i] == '?') {
            questionCount++;
        }
    }

    std::cout << questionCount << std::endl;
    return 0;
}
```

</details>

---

## **ЗАДАЧА 5: Повече четни или нечетни?**

От стандартния вход се въвеждат `n` (n ≤ 60) цели числа. Програмата трябва да изведе:

- "More even!" ако четните числа са повече
- "More odd!" ако нечетните числа са повече
- "Equal!" ако са равен брой

**Примери:**

```
Input:
5
2 3 4 5 6

Output: More even!
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int arr[60];
    for(int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    int evenCount = 0;
    int oddCount = 0;

    for(int i = 0; i < n; i++) {
        if(arr[i] % 2 == 0) {
            evenCount++;
        } else {
            oddCount++;
        }
    }

    if(evenCount > oddCount) {
        std::cout << "More even!" << std::endl;
    } else if(oddCount > evenCount) {
        std::cout << "More odd!" << std::endl;
    } else {
        std::cout << "Equal!" << std::endl;
    }

    return 0;
}
```

</details>

---

## **ЗАДАЧА 6: Броене на цифри в текст**

От стандартния вход се въвежда символен низ с не повече от 200 символа, съдържащ латински букви, цифри, интервали и препинателни знаци. Напишете програма, която извежда колко отделни числа има в текста.

**Отделно число** е всяка последователност от цифри, разделена с интервали, букви, препинателни знаци или краищата на низа.

**Примери:**

```
Input: "I have 2 apples and 15 oranges."
Output: 2

Input: "100 200 300"
Output: 3

Input: "abc123def456"
Output: 2
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    char text[201];
    std::cin.getline(text, 201);

    int numberCount = 0;
    bool inNumber = false;

    for(int i = 0; text[i] != '\0'; i++) {
        if(text[i] >= '0' && text[i] <= '9') {
            if(!inNumber) {
                numberCount++;
                inNumber = true;
            }
        } else {
            inNumber = false;
        }
    }

    std::cout << numberCount << std::endl;
    return 0;
}
```

</details>

---

## **ЗАДАЧА 7: Проверка за нарастваща редица**

От стандартния вход се въвежда число `n` (1 ≤ n ≤ 30), последвано от n цели числа. Напишете програма, която проверява дали числата са подредени в строго нарастващ ред. Изведете "YES!" ако са нарастващи, иначе "NO!".

**Примери:**

```
Input:
4
1 3 5 9

Output: YES!

Input:
4
1 3 3 9

Output: NO!
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int arr[30];
    for(int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    bool increasing = true;
    for(int i = 0; i < n - 1; i++) {
        if(arr[i] >= arr[i + 1]) {
            increasing = false;
            break;
        }
    }

    if(increasing) {
        std::cout << "YES!" << std::endl;
    } else {
        std::cout << "NO!" << std::endl;
    }

    return 0;
}
```

</details>

## **ЗАДАЧА 8**

От стандартния вход се въвежда число `N` (1 ≤ N ≤ 15), последвано от N реда, всеки с N цели числа. Напишете програма, която извежда броя на редовете, в които числата са подредени в строго низходящ ред.

> Може да не използвате масиви

**Примери:**

```
Input:
4
9 7 5 3
1 2 3 4
8 6 4 2
5 5 5 5

Output: 2
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int decreasingRows = 0;

    for(int i = 0; i < n; i++) {
        int prev, current;
        std::cin >> prev;

        bool isDecreasing = true;
        for(int j = 1; j < n; j++) {
            std::cin >> current;
            if(prev <= current) {
                isDecreasing = false;
            }
            prev = current;
        }

        if(isDecreasing) {
            decreasingRows++;
        }
    }

    std::cout << decreasingRows << std::endl;
    return 0;
}
```

</details>

---

## **ЗАДАЧА 9: Сравнение на суми**

От стандартния вход се въвеждат `n` (n ≤ 70) цели числа. Напишете програма, която извежда "Positive wins!" ако сумата на положителните числа е по-голяма от абсолютната стойност на сумата на отрицателните числа, иначе извежда "Negative wins or equal!".

**Примери:**

```
Input:
4
5 -2 3 -1

Output: Positive wins!

Input:
4
5 -5 2 -2

Output: Negative wins or equal!
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int arr[70];
    for(int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    int positiveSum = 0;
    int negativeSum = 0;

    for(int i = 0; i < n; i++) {
        if(arr[i] > 0) {
            positiveSum += arr[i];
        } else if(arr[i] < 0) {
            negativeSum += arr[i];
        }
    }

    int absNegativeSum = (negativeSum < 0) ? -negativeSum : negativeSum;

    if(positiveSum > absNegativeSum) {
        std::cout << "Positive wins!" << std::endl;
    } else {
        std::cout << "Negative wins or equal!" << std::endl;
    }

    return 0;
}
```

</details>

---

## **ЗАДАЧА 10: Броене на редици с константна разлика**

От стандартния вход се въвежда число `N` (1 ≤ N ≤ 18), последвано от N реда, всеки с N цели числа. Напишете програма, която извежда броя на редовете, в които числата образуват аритметична прогресия (разликата между всеки два съседни елемента е константна).

**Примери:**

```
Input:
4
2 4 6 8
1 3 7 9
5 5 5 5
10 7 4 1

Output: 3
```

_(Редове 1, 3 и 4 са аритметични прогресии)_

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int matrix[18][18];
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            std::cin >> matrix[i][j];
        }
    }

    int arithmeticRows = 0;
    for(int i = 0; i < n; i++) {
        bool isArithmetic = true;
        if(n > 1) {
            int diff = matrix[i][1] - matrix[i][0];
            for(int j = 1; j < n - 1; j++) {
                if(matrix[i][j + 1] - matrix[i][j] != diff) {
                    isArithmetic = false;
                    break;
                }
            }
        }
        if(isArithmetic) {
            arithmeticRows++;
        }
    }

    std::cout << arithmeticRows << std::endl;
    return 0;
}
```

</details>

---

## **ЗАДАЧА 11: Обединяване на два масива**

От стандартния вход се въвеждат две числа `n` и `m` (1 ≤ n, m ≤ 25), последвани от n числа (първи масив) и m числа (втори масив). Напишете програма, която проверява дали редицата, получена чрез редуване на елементите от двата масива (a1, b1, a2, b2, ...), е подредена в нарастващ ред. Изведете "Sorted!" ако е сортирана, иначе "Not sorted!".

**Примери:**

```
Input:
3
3
1 3 5
2 4 6

Output: Sorted!
// (Редицата е: 1 2 3 4 5 6)

Input:
2
2
5 1
2 3

Output: Not sorted!
// (Редицата е: 5 2 1 3)
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n, m;
    std::cin >> n >> m;

    int arr1[25], arr2[25];
    for(int i = 0; i < n; i++) {
        std::cin >> arr1[i];
    }
    for(int i = 0; i < m; i++) {
        std::cin >> arr2[i];
    }

    int merged[50];
    int idx = 0;
    int maxLen = (n > m) ? n : m;

    for(int i = 0; i < maxLen; i++) {
        if(i < n) {
            merged[idx++] = arr1[i];
        }
        if(i < m) {
            merged[idx++] = arr2[i];
        }
    }

    bool sorted = true;
    for(int i = 0; i < idx - 1; i++) {
        if(merged[i] >= merged[i + 1]) {
            sorted = false;
            break;
        }
    }

    if(sorted) {
        std::cout << "Sorted!" << std::endl;
    } else {
        std::cout << "Not sorted!" << std::endl;
    }

    return 0;
}
```

</details>

---

## **ЗАДАЧА 12: Проверка за огледални половини**

От стандартния вход се въвежда четно число `n` (2 ≤ n ≤ 40, n е четно), последвано от n цели числа. Напишете програма, която проверява дали първата половина на масива е същата като втората половина. Изведете "Mirror!" ако са еднакви, иначе "Different!".

**Примери:**

```
Input:
6
1 2 3 1 2 3

Output: Mirror!

Input:
4
5 6 7 8

Output: Different!
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int arr[40];
    for(int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    bool mirror = true;
    int half = n / 2;

    for(int i = 0; i < half; i++) {
        if(arr[i] != arr[half + i]) {
            mirror = false;
            break;
        }
    }

    if(mirror) {
        std::cout << "Mirror!" << std::endl;
    } else {
        std::cout << "Different!" << std::endl;
    }

    return 0;
}
```

</details>

---

## **ЗАДАЧА 13: Най-дълга последователност от еднакви символи**

От стандартния вход се въвежда символен низ с не повече от 100 символа. Напишете програма, която намира най-дългата последователност от еднакви последователни символи и извежда нейната дължина.

**Примери:**

```
Input: "aaabbbcc"
Output: 3

Input: "abcdef"
Output: 1

Input: "aabbbbaa"
Output: 4
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    char text[101];
    std::cin >> text;

    int maxLen = 1;
    int currentLen = 1;

    for(int i = 1; text[i] != '\0'; i++) {
        if(text[i] == text[i - 1]) {
            currentLen++;
            if(currentLen > maxLen) {
                maxLen = currentLen;
            }
        } else {
            currentLen = 1;
        }
    }

    std::cout << maxLen << std::endl;
    return 0;
}
```

</details>

---

## **ЗАДАЧА 14: Проверка за симетрична матрица**

От стандартния вход се въвежда число `N` (1 ≤ N ≤ 20), последвано от N реда, всеки с N цели числа. Напишете програма, която проверява дали матрицата е симетрична (елементът на позиция [i][j] е равен на елемента на позиция [j][i]). Изведете "Symmetric!" или "Not symmetric!".

**Примери:**

```
Input:
3
1 2 3
2 5 6
3 6 9

Output: Symmetric!

Input:
2
1 2
3 4

Output: Not symmetric!
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int matrix[20][20];
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            std::cin >> matrix[i][j];
        }
    }

    bool symmetric = true;
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            if(matrix[i][j] != matrix[j][i]) {
                symmetric = false;
                break;
            }
        }
        if(!symmetric) break;
    }

    if(symmetric) {
        std::cout << "Symmetric!" << std::endl;
    } else {
        std::cout << "Not symmetric!" << std::endl;
    }

    return 0;
}
```

</details>

---

## **ЗАДАЧА 15: Броене на главни букви в изречения**

От стандартния вход се въвежда символен низ с не повече от 255 символа, съдържащ латински букви, интервали и препинателни знаци ('.', '!', '?'). Напишете програма, която извежда броя на изреченията, които започват с главна буква.

**Примери:**

```
Input: "Hello! how are you? Fine."
Output: 2

Input: "this is a test. Another test!"
Output: 1
```

<details>
<summary><b>📝 Покажи решение</b></summary>

```cpp
#include <iostream>

int main() {
    char text[256];
    std::cin.getline(text, 256);

    int count = 0;
    bool startOfSentence = true;

    for(int i = 0; text[i] != '\0'; i++) {
        if(startOfSentence) {
            if(text[i] >= 'A' && text[i] <= 'Z') {
                count++;
            }
            startOfSentence = false;
        }

        if(text[i] == '.' || text[i] == '!' || text[i] == '?') {
            startOfSentence = true;
        } else if(text[i] != ' ') {
            startOfSentence = false;
        }
    }

    std::cout << count << std::endl;
    return 0;
}
```

</details>

---

## **ОБОБЩЕНИЕ И СЪВЕТИ**

### **Основни Pattern-и в задачите:**

1. **Броене на елементи с условие** - използвайте брояч и проверка
2. **Проверка за нарастване/намаляване** - сравнете съседни елементи
3. **Обработка на символни низове** - използвайте флагове (bool)
4. **Работа с матрици** - вложени цикли
5. **Сравнение на части от масиви** - разделете на половини/части

### **Често срещани грешки:**

- ❌ Забравяне на `'\0'` при работа с char масиви
- ❌ Грешки при граничните случаи (индекс извън границите)
- ❌ Неправилна инициализация на променливи
- ❌ Забравяне на `std::cin.getline()` при четене на низове с интервали

### **Checklist преди тест:**

- [ ] Умеете да броите елементи с определено свойство
- [ ] Можете да обхождате символни низове
- [ ] Разбирате как да работите с матрици
- [ ] Знаете как да сравнявате части от масиви
- [ ] Можете да проверявате за нарастващи/намаляващи редици
- [ ] Разбирате аритметични прогресии
- [ ] Умеете да работите с bool флагове

**Успех на теста! 🚀**
