# **ЗАДАЧИ - СЕДМИЦА 7**

## **Символни низове (char arrays) - Основни операции**

---

## 🔰 Ниво 1: Основи

---

## Задача 0: Първа програма с низове

Напишете програма, която приема текстови вход от конзолата (една дума) и извежда:

1. Целия низ
2. Първия символ
3. Последния символ
4. Дължината на низа (без да използвате `strlen`)

```c++
// Вход:
FMI
// Изход:
Word: FMI
First: F
Last: I
Length: 3
```

> **Подсказка:** Обходете низа докато не стигнете '\0'

---

## Задача 1: Посимволно извеждане

Напишете програма, която приема текстови вход от конзолата и отпечатва всеки негов знак на нов ред.

```c++
// Вход:
Hello!
// Изход:
H
e
l
l
o
!
```

---

## Задача 2: Дължина на низ

Напишете функция `myStrlen`, която приема символен низ и връща неговата дължина като `int`. **НЕ** използвайте `strlen` от `<cstring>`.

```c++
myStrlen("12345");      // 5
myStrlen("FMI");        // 3
myStrlen("");           // 0
```

**Сигнатура:**

```c++
int myStrlen(const char* str);
```

---

## Задача 3: Съдържа ли се символ?

Напишете функция `contains`, която като аргументи приема низ и символ. Функцията връща `true`, ако символът се съдържа в низа и `false` - ако не се.

```c++
contains("ABCD", 'c');      // false (case-sensitive!)
contains("ABCD", 'C');      // true
contains("Hello", 'l');     // true
contains("FMI", 'x');       // false
```

**Сигнатура:**

```c++
bool contains(const char* str, char ch);
```

---

## Задача 4: Броене на срещания

Напишете функция `countOccurrences`, която приема низ и символ, и връща колко пъти се среща символът в низа.

```c++
countOccurrences("Hello", 'l');     // 2
countOccurrences("FMI", 'M');       // 1
countOccurrences("Mississippi", 's'); // 4
countOccurrences("ABC", 'x');       // 0
```

**Сигнатура:**

```c++
int countOccurrences(const char* str, char ch);
```

---

## 🔸 Ниво 2: Трансформации

---

## Задача 5: Копиране на низ

Напишете функция `myStrcpy`, която копира символите от `source` в `destination` (включително '\0'). **НЕ** използвайте `strcpy`.

```c++
char src[] = "Hello";
char dest[50];
myStrcpy(dest, src);
// dest = "Hello"
```

**Сигнатура:**

```c++
void myStrcpy(char* dest, const char* src);
```

> **⚠️ Важно:** Приемайте, че `dest` има достатъчно място!

---

## Задача 6: Обръщане на низ

Напишете функция `reverseString`, която обръща символите в даден низ (модифицира оригиналния низ).

```c++
char text[] = "Hello";
reverseString(text);
// text = "olleH"
```

**Още примери:**

```c++
reverseString("FMI");       // "IMF"
reverseString("abcd");      // "dcba"
reverseString("12321");     // "12321"
```

**Сигнатура:**

```c++
void reverseString(char* str);
```

> **Подсказка:** Разменете елементите симетрично (първи с последен, втори с предпоследен...)

---

## Задача 7: Главни и малки букви

Напишете три функции, които приемат символен низ и го трансформират (модифицират оригинала):

### 1. `toUpperCase()` - Превръща всички букви в главни

```c++
char text[] = "Hello World";
toUpperCase(text);
// text = "HELLO WORLD"
```

### 2. `toLowerCase()` - Превръща всички букви в малки

```c++
char text[] = "Hello World";
toLowerCase(text);
// text = "hello world"
```

### 3. `toggleCase()` - Обръща регистъра (главни→малки, малки→главни)

```c++
char text[] = "HeLLo WoRLd";
toggleCase(text);
// text = "hEllO wOrlD"
```

**Сигнатури:**

```c++
void toUpperCase(char* str);
void toLowerCase(char* str);
void toggleCase(char* str);
```

> **⚠️ Важно:** Само букви се променят! Цифри, интервали и специални символи остават непроменени.

---

## Задача 8: Редуващи се малки и главни букви

Напишете функция `alternateCase`, която прави буквите в низа редуващи се: първата буква главна, втората малка, третата главна и т.н.

**⚠️ Важно:** Интервалите и небуквените символи **НЕ** се броят в редуването!

```c++
char text1[] = "hello";
alternateCase(text1);
// text1 = "HeLlO"

char text2[] = "i like fmi";
alternateCase(text2);
// text2 = "I LiKe FmI"

char text3[] = "abc123def";
alternateCase(text3);
// text3 = "AbC123dEf"
```

**Сигнатура:**

```c++
void alternateCase(char* str);
```

---

## 🔷 Ниво 3: Сложни операции

---

## Задача 9: Извличане на подниз

Напишете функция `getSubstring`, която приема като аргументи низ, начален и краен индекс. Функцията копира подниза в `result`.

```c++
char text[] = "123456";
char result[50];

getSubstring(text, result, 0, 2);  // result = "123"
getSubstring(text, result, 3, 5);  // result = "456"
getSubstring(text, result, 1, 3);  // result = "234"
```

**Сигнатура:**

```c++
void getSubstring(const char* str, char* result, int start, int end);
```

> **Забележка:** Индексите `start` и `end` са **включващи** (от start до end, включително)

---

## Задача 10: Премахване на интервали

Напишете функция `removeSpaces`, която премахва всички интервали от низ (модифицира оригинала).

```c++
char text1[] = "Hello World";
removeSpaces(text1);
// text1 = "HelloWorld"

char text2[] = "  I   like   FMI  ";
removeSpaces(text2);
// text2 = "IlikeFMI"
```

**Сигнатура:**

```c++
void removeSpaces(char* str);
```

> **Подсказка:** Използвайте два индекса - един за четене, един за писане.

---

## Задача 11: CamelCase конвертор

Напишете функция `toCamelCase`, която приема символен низ и го трансформира по следния начин:

1. Премахва всички интервали
2. Прави всички букви малки
3. Всяка първа буква от дума става главна

```c++
char text1[] = "i lIKe fMI";
toCamelCase(text1);
// text1 = "ILikeFmi"

char text2[] = "hello WORLD from   FMI";
toCamelCase(text2);
// text2 = "HelloWorldFromFmi"

char text3[] = "Matematika Ura";
toCamelCase(text3);
// text3 = "MatematikaUra"
```

**Сигнатура:**

```c++
void toCamelCase(char* str);
```

---

## Задача 12: Размяна по двойки

Напишете функция `swapPairs`, която разменя символите на низ по двойки (съседни елементи). Когато низът е с нечетен брой символи, последният елемент остава на мястото си.

```c++
char text1[] = "123456";
swapPairs(text1);
// text1 = "214365"

char text2[] = "1234567";
swapPairs(text2);
// text2 = "2143657"

char text3[] = "Hello";
swapPairs(text3);
// text3 = "eHll o"
```

**Сигнатура:**

```c++
void swapPairs(char* str);
```

---

## 🔶 Ниво 4: Алгоритмични задачи

---

## Задача 13: Проверка за палиндром

Напишете функция `isPalindrome`, която проверява дали даден низ е палиндром (чете се еднакво отляво надясно и отдясно наляво).

```c++
isPalindrome("racecar");    // true
isPalindrome("hello");      // false
isPalindrome("12321");      // true
isPalindrome("A");          // true
isPalindrome("abba");       // true
isPalindrome("abcd");       // false
```

**Сигнатура:**

```c++
bool isPalindrome(const char* str);
```

> **Подсказка:** Сравнявайте символите от двата края навътре.

---

## Задача 14: Премахване на дубликати

Напишете функция `removeDuplicates`, която премахва всички повтарящи се символи от низ - запазва само първото срещане на всеки символ.

```c++
char text1[] = "hello";
removeDuplicates(text1);
// text1 = "helo"

char text2[] = "aabbccdd";
removeDuplicates(text2);
// text2 = "abcd"

char text3[] = "Mississippi";
removeDuplicates(text3);
// text3 = "Misp"
```

**Сигнатура:**

```c++
void removeDuplicates(char* str);
```

> **Подсказка:** Използвайте булев масив за отбелязване на видени символи (ASCII има 128/256 символа).

---

## Задача 15: Индексиране на повторения

Напишете функция `indexRepeats`, която след всяка буква в низа добавя индекс показващ номера на срещане на съответната буква (започвайки от 1).

**⚠️ За улеснение:** Малките и главните букви се третират като **различни** символи.

```c++
char text1[] = "abcabc";
char result1[100];
indexRepeats(text1, result1);
// result1 = "a1b1c1a2b2c2"

char text2[] = "hello";
char result2[100];
indexRepeats(text2, result2);
// result2 = "h1e1l1l2o1"

char text3[] = "aaa";
char result3[100];
indexRepeats(text3, result3);
// result3 = "a1a2a3"
```

**Сигнатура:**

```c++
void indexRepeats(const char* str, char* result);
```

> **Подсказка:** Използвайте масив за броене на срещанията на всеки символ (за 'A'-'Z' и 'a'-'z').

---

## 🔴 Ниво 5: Напреднали задачи

---

## Задача 16: Анаграми

Напишете функция `areAnagrams`, която проверява дали два низа са анаграми (съдържат едни и същи букви със същите честоти, но в различен ред).

```c++
areAnagrams("listen", "silent");    // true
areAnagrams("hello", "world");      // false
areAnagrams("abc", "bca");          // true
areAnagrams("aab", "aba");          // true
areAnagrams("aab", "aaa");          // false
```

**Сигнатура:**

```c++
bool areAnagrams(const char* str1, const char* str2);
```

> **Подсказка:** Преброете честотите на символите в двата низа и ги сравнете.

---

## Задача 17: Най-дълга дума

Напишете функция `findLongestWord`, която намира най-дългата дума в изречение (низ съдържащ множество думи разделени с интервали) и я копира в `result`.

```c++
char text1[] = "I love programming in C++";
char longest1[50];
findLongestWord(text1, longest1);
// longest1 = "programming"

char text2[] = "FMI is the best";
char longest2[50];
findLongestWord(text2, longest2);
// longest2 = "best" (или "FMI" - ако са равни, вземете първата)
```

**Сигнатура:**

```c++
void findLongestWord(const char* str, char* result);
```

---

## Задача 18: Броене на думи

Напишете функция `countWords`, която брои колко думи има в даден низ. Думите са разделени с един или повече интервали.

```c++
countWords("Hello World");           // 2
countWords("I   love   FMI");        // 3
countWords("   FMI   ");             // 1
countWords("OneWord");               // 1
countWords("   ");                   // 0
```

**Сигнатура:**

```c++
int countWords(const char* str);
```

> **Подсказка:** Дума започва когато срещнете буква след интервал (или в началото).

---

## Задача 19: Цезаров шифър

Напишете функция `caesarCipher`, която прилага [Цезаров шифър](https://bg.wikipedia.org/wiki/%D0%A8%D0%B8%D1%84%D1%8A%D1%80_%D0%BD%D0%B0_%D0%A6%D0%B5%D0%B7%D0%B0%D1%80) върху низ - всяка буква се замества с буквата на `shift` позиции по-нататък в азбуката (циклично).

```c++
char text1[] = "ABC";
caesarCipher(text1, 1);
// text1 = "BCD"

char text2[] = "XYZ";
caesarCipher(text2, 3);
// text2 = "ABC" (циклично)

char text3[] = "Hello World";
caesarCipher(text3, 5);
// text3 = "Mjqqt Btwqi"
```

**Сигнатура:**

```c++
void caesarCipher(char* str, int shift);
```

> **⚠️ Важно:**
>
> - Главните букви остават главни, малките - малки
> - Небуквените символи не се променят
> - Shift може да е отрицателно число

---

## Задача 20: Валидация на паролa

Напишете функция `isValidPassword`, която проверява дали дадена парола отговаря на следните критерии:

- Дължина поне 8 символа
- Съдържа поне една главна буква
- Съдържа поне една малка буква
- Съдържа поне една цифра

```c++
isValidPassword("Pass1234");        // true
isValidPassword("password");        // false (няма главна буква и цифра)
isValidPassword("PASSWORD123");     // false (няма малка буква)
isValidPassword("Pass");            // false (твърде кратка)
isValidPassword("MyP@ssw0rd");      // true
```

**Сигнатура:**

```c++
bool isValidPassword(const char* password);
```

---

## 🎯 Бонус задачи

---

## Бонус 1: Сортиране на символите в низ

Напишете функция `sortString`, която сортира символите в низ по азбучен ред (по ASCII стойност).

```c++
char text1[] = "dcba";
sortString(text1);
// text1 = "abcd"

char text2[] = "FMI2024";
sortString(text2);
// text2 = "0224FIM"
```

**Сигнатура:**

```c++
void sortString(char* str);
```

> **Подсказка:** Използвайте Bubble Sort или Selection Sort.

---

## Бонус 2: Компресиране на низ

Напишете функция `compressString`, която компресира низ като замества последователни повторения с буква + брой.

```c++
char text1[] = "aaabbc";
char result1[50];
compressString(text1, result1);
// result1 = "a3b2c1"

char text2[] = "aabbccdd";
char result2[50];
compressString(text2, result2);
// result2 = "a2b2c2d2"

char text3[] = "abc";
char result3[50];
compressString(text3, result3);
// result3 = "a1b1c1" (или "abc" ако искате оптимизация)
```

**Сигнатура:**

```c++
void compressString(const char* str, char* result);
```

---

**Успех с упражненията! 💪🚀**
