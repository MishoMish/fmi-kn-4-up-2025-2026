# **ТЕОРИЯ - СТРУКТУРИ, КЛАСОВЕ И ENUMS**

---

# _КАКВО СА СТРУКТУРИТЕ?_

## Основни понятия

**Структурата** е потребителски дефиниран тип данни, който позволява групиране на **свързани данни от различни типове** под едно име. Това е основен начин за организация на данни в C++.

### Защо използваме структури?

1. ✅ **Организация на данни** - логическо групиране на свързана информация
2. ✅ **Четимост** - кодът става по-разбираем и поддържаем
3. ✅ **Многократна употреба** - една структура може да се използва многократно
4. ✅ **Моделиране на реални обекти** - точка, студент, книга, и т.н.
5. ✅ **Основа за ООП** - структурите са основа за класовете

---

## 1) ОСНОВИ НА СТРУКТУРИТЕ

### Синтаксис:

```cpp
struct ИмеНаСтруктура {
    тип1 член1;
    тип2 член2;
    тип3 член3;
    // ...
};  // ⚠️ НЕ ЗАБРАВЯЙТЕ ; В КРАЯ!
```

### Пример - структура Точка:

```cpp
struct Point {
    int x;
    int y;
};
```

**Визуализация в паметта:**

```
┌─────────────┬──────────┐
│    Име      │ Стойност │
├─────────────┼──────────┤
│  Point::x   │    ?     │  ← 4 bytes (int)
├─────────────┼──────────┤
│  Point::y   │    ?     │  ← 4 bytes (int)
└─────────────┴──────────┘
Общо: 8 bytes
```

---

## 2) СЪЗДАВАНЕ НА ПРОМЕНЛИВИ ОТ СТРУКТУРЕН ТИП

### Начини за деклариране:

```cpp
// 1. Обикновена декларация
Point p1;

// 2. Декларация с инициализация (брой стойности = брой членове)
Point p2 = {10, 20};

// 3. Декларация с частична инициализация
Point p3 = {5};  // x = 5, y = 0 (останалите се нулират)

// 4. Декларация с празна инициализация
Point p4 = {};   // x = 0, y = 0

// 5. C++11 и нагоре - uniform initialization
Point p5{15, 25};

// 6. Масив от структури
Point points[100];

// 7. Динамична алокация
Point* ptr = new Point;
Point* arr = new Point[10];
```

### Пример:

```cpp
#include <iostream>

struct Student {
    char name[50];
    int fn;
    double grade;
};

int main() {
    Student s1;  // Недефинирани стойности
    Student s2 = {"Ivan Petrov", 12345, 5.50};
    Student s3 = {};  // Всички полета = 0/null

    return 0;
}
```

---

## 3) ДОСТЪП ДО ЧЛЕНОВЕ НА СТРУКТУРА

### Оператор `.` (точка):

```cpp
struct Point {
    int x;
    int y;
};

int main() {
    Point p;

    // Запис на стойности
    p.x = 10;
    p.y = 20;

    // Четене на стойности
    std::cout << "x = " << p.x << std::endl;
    std::cout << "y = " << p.y << std::endl;

    return 0;
}
```

### Оператор `->` (за указатели):

```cpp
int main() {
    Point p = {10, 20};
    Point* ptr = &p;

    // Достъп чрез указател - 2 начина:

    // 1. С оператор ->
    std::cout << ptr->x << std::endl;  // 10
    std::cout << ptr->y << std::endl;  // 20

    // 2. С дереференция и .
    std::cout << (*ptr).x << std::endl;  // 10
    std::cout << (*ptr).y << std::endl;  // 20

    return 0;
}
```

**Визуализация:**

```
Стек:                    Памет:
┌──────────┐            ┌────────┐
│ p.x = 10 │            │ x: 10  │ ← 0x1000
├──────────┤            ├────────┤
│ p.y = 20 │            │ y: 20  │ ← 0x1004
└──────────┘            └────────┘
                             ↑
┌──────────┐                 │
│ ptr = ●──┼─────────────────┘
└──────────┘

ptr->x  означава  "отиди на адреса в ptr и вземи полето x"
```

---

## 4) СТРУКТУРИ И ФУНКЦИИ

### Подаване по стойност (copy):

```cpp
struct Rectangle {
    int width;
    int height;
};

// ❌ НЕЕФЕКТИВНО - копира се цялата структура!
int area(Rectangle r) {
    return r.width * r.height;
}

int main() {
    Rectangle rect = {10, 5};
    int a = area(rect);  // Копира rect → slow за големи структури
    std::cout << a << std::endl;  // 50
    return 0;
}
```

**Визуализация на копирането:**

```
main():                   area():
┌──────────────┐         ┌──────────────┐
│ rect.width=10│  копия  │ r.width = 10 │
├──────────────┤  ────→  ├──────────────┤
│ rect.height=5│         │ r.height = 5 │
└──────────────┘         └──────────────┘
    Original                 Copy
```

### Подаване по референция (не се копира):

```cpp
// ✅ ЕФЕКТИВНО - не се копира, използва се оригинала
int area(const Rectangle& r) {
    return r.width * r.height;
}

// Може и да променяме оригинала
void scale(Rectangle& r, int factor) {
    r.width *= factor;
    r.height *= factor;
}

int main() {
    Rectangle rect = {10, 5};
    int a = area(rect);        // Не се копира!
    scale(rect, 2);            // rect става {20, 10}
    std::cout << rect.width << std::endl;  // 20
    return 0;
}
```

### Връщане на структура:

```cpp
Point createPoint(int x, int y) {
    Point p;
    p.x = x;
    p.y = y;
    return p;  // Връща копие на структурата
}

// Алтернатива - връщане чрез параметър
void createPoint(int x, int y, Point& result) {
    result.x = x;
    result.y = y;
}
```

---

## 5) ВЛАГАНЕ НА СТРУКТУРИ

Структурите могат да съдържат други структури като членове.

### Пример:

```cpp
struct Date {
    int day;
    int month;
    int year;
};

struct Student {
    char name[50];
    int fn;
    Date birthDate;  // Вложена структура
};

int main() {
    Student s;

    // Достъп до вложена структура
    s.birthDate.day = 15;
    s.birthDate.month = 6;
    s.birthDate.year = 2003;

    std::cout << s.birthDate.day << "."
              << s.birthDate.month << "."
              << s.birthDate.year << std::endl;

    return 0;
}
```

**Визуализация в паметта:**

```
Student s:
┌──────────────────────┐
│ name[50]             │  ← 50 bytes
├──────────────────────┤
│ fn                   │  ← 4 bytes
├──────────────────────┤
│ birthDate:           │
│   ├─ day (4 bytes)   │
│   ├─ month (4 bytes) │
│   └─ year (4 bytes)  │  ← 12 bytes общо
└──────────────────────┘
Общо: 66 bytes (+ padding)
```

---

## 6) МАСИВИ ОТ СТРУКТУРИ

### Статични масиви:

```cpp
struct Book {
    char title[100];
    char author[50];
    double price;
};

int main() {
    Book library[100];

    // Инициализация на първата книга
    strcpy(library[0].title, "C++ Programming");
    strcpy(library[0].author, "Bjarne Stroustrup");
    library[0].price = 59.99;

    // Обхождане
    for (int i = 0; i < 100; i++) {
        if (library[i].price > 0) {
            std::cout << library[i].title << std::endl;
        }
    }

    return 0;
}
```

### Динамични масиви:

```cpp
int main() {
    int n;
    std::cin >> n;

    Book* books = new Book[n];

    // Работа с масива
    for (int i = 0; i < n; i++) {
        std::cin >> books[i].title;
        std::cin >> books[i].author;
        std::cin >> books[i].price;
    }

    // Освобождаване на паметта
    delete[] books;

    return 0;
}
```

---

## 7) РАЗМЕР НА СТРУКТУРИТЕ (PADDING)

### Проблемът с подравняването:

```cpp
struct Example1 {
    char a;      // 1 byte
    int b;       // 4 bytes
    char c;      // 1 byte
};

std::cout << sizeof(Example1) << std::endl;  // Вероятно 12, не 6!
```

**Визуализация на padding:**

```
Без padding (теоретично):
┌────┬────────────────┬────┐
│ a  │       b        │ c  │
│ 1B │      4B        │ 1B │
└────┴────────────────┴────┘
Общо: 6 bytes

С padding (реално):
┌────┬───┬───┬───┬────────────────┬────┬───┬───┬───┐
│ a  │ P │ P │ P │       b        │ c  │ P │ P │ P │
│ 1B │ 1 │ 1 │ 1 │      4B        │ 1B │ 1 │ 1 │ 1 │
└────┴───┴───┴───┴────────────────┴────┴───┴───┴───┘
Общо: 12 bytes (P = padding)
```

### Оптимизация - подредба по размер:

```cpp
// ❌ Лошо - много padding
struct Bad {
    char a;      // 1 byte
    double b;    // 8 bytes
    char c;      // 1 byte
    int d;       // 4 bytes
};  // sizeof() = 24 bytes

// ✅ Добре - минимален padding
struct Good {
    double b;    // 8 bytes
    int d;       // 4 bytes
    char a;      // 1 byte
    char c;      // 1 byte
};  // sizeof() = 16 bytes
```

**Правило:** Подреждайте членовете от най-големите към най-малките!

---

## 8) TYPEDEF И СТРУКТУРИ

### Опростяване на синтаксиса:

```cpp
// Вариант 1 - класически начин
struct Point {
    int x;
    int y;
};

// Използване:
struct Point p1;  // В C трябва да пишем 'struct'
Point p2;         // В C++ можем без 'struct'

// Вариант 2 - с typedef
typedef struct {
    int x;
    int y;
} Point;

// Вариант 3 - typedef след дефиниция
struct PointStruct {
    int x;
    int y;
};
typedef struct PointStruct Point;

// Вариант 4 - C++11 using (препоръчително)
using Point = struct {
    int x;
    int y;
};
```

---

# _КАКВО СА КЛАСОВЕТЕ?_

## 1) КЛАСОВЕ VS СТРУКТУРИ

### Основна разлика:

```cpp
// СТРУКТУРА - по подразбиране public
struct StructExample {
    int x;        // public по подразбиране
    int y;        // public по подразбиране
};

// КЛАС - по подразбиране private
class ClassExample {
    int x;        // private по подразбиране
    int y;        // private по подразбиране
};
```

### Контрол на достъпа:

```cpp
class Point {
private:           // Само вътре в класа
    int x;
    int y;

public:            // Достъпно отвън
    void setX(int newX) {
        x = newX;
    }

    int getX() {
        return x;
    }

protected:         // В класа и наследниците
    int z;
};

int main() {
    Point p;
    // p.x = 10;       // ❌ ГРЕШКА! x е private
    p.setX(10);        // ✅ OK - setX е public
    std::cout << p.getX() << std::endl;  // 10
    return 0;
}
```

---

## 2) ЕНКАПСУЛАЦИЯ (ENCAPSULATION)

### Защо скриваме данните?

```cpp
class BankAccount {
private:
    double balance;  // Скрито - не може директен достъп

public:
    // Контролиран достъп чрез методи
    void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
        }
    }

    bool withdraw(double amount) {
        if (amount > 0 && amount <= balance) {
            balance -= amount;
            return true;
        }
        return false;
    }

    double getBalance() const {
        return balance;
    }
};

int main() {
    BankAccount acc;
    // acc.balance = 1000000;  // ❌ ГРЕШКА! Не може директно
    acc.deposit(100);          // ✅ Контролиран начин
    acc.withdraw(50);
    std::cout << acc.getBalance() << std::endl;  // 50
    return 0;
}
```

**Предимства:**

- ✅ Защита на данните от невалидни стойности
- ✅ Контрол над промените
- ✅ Възможност за валидация
- ✅ Лесна промяна на вътрешната имплементация

---

## 3) КОНСТРУКТОРИ

### Какво е конструктор?

Конструкторът е **специална функция**, която се извиква автоматично при създаване на обект. Използва се за инициализация.

```cpp
class Point {
private:
    int x;
    int y;

public:
    // Конструктор по подразбиране
    Point() {
        x = 0;
        y = 0;
    }

    // Конструктор с параметри
    Point(int newX, int newY) {
        x = newX;
        y = newY;
    }

    // Копиращ конструктор
    Point(const Point& other) {
        x = other.x;
        y = other.y;
    }
};

int main() {
    Point p1;           // Извиква Point()
    Point p2(10, 20);   // Извиква Point(int, int)
    Point p3 = p2;      // Извиква Point(const Point&)
    Point p4{5, 15};    // C++11 uniform initialization

    return 0;
}
```

### Initialization List (по-ефективно):

```cpp
class Point {
private:
    int x;
    int y;

public:
    // ✅ Препоръчителен начин - initialization list
    Point() : x(0), y(0) {
        // Празно тяло
    }

    Point(int newX, int newY) : x(newX), y(newY) {
        // x и y са вече инициализирани
    }
};
```

---

## 4) ДЕСТРУКТОРИ

### Какво е деструктор?

Деструкторът е **специална функция**, която се извиква автоматично при **унищожаване** на обект. Използва се за освобождаване на ресурси.

```cpp
class DynamicArray {
private:
    int* data;
    int size;

public:
    // Конструктор - заделя памет
    DynamicArray(int n) {
        size = n;
        data = new int[size];
        std::cout << "Allocated " << size << " ints" << std::endl;
    }

    // Деструктор - освобождава памет
    ~DynamicArray() {
        delete[] data;
        std::cout << "Freed memory" << std::endl;
    }
};

int main() {
    {
        DynamicArray arr(100);
        // Работа с масива...
    }  // ← Тук автоматично се извиква ~DynamicArray()

    return 0;
}
```

**Изход:**

```
Allocated 100 ints
Freed memory
```

---

## 5) МЕТОДИ (ФУНКЦИИ В КЛАСОВЕ)

### Декларация и дефиниция:

```cpp
class Rectangle {
private:
    int width;
    int height;

public:
    // Метод дефиниран вътре в класа (inline)
    int area() {
        return width * height;
    }

    // Декларация на метод
    int perimeter();

    // const метод - не променя обекта
    void display() const;
};

// Дефиниция извън класа
int Rectangle::perimeter() {
    return 2 * (width + height);
}

void Rectangle::display() const {
    std::cout << "Width: " << width
              << ", Height: " << height << std::endl;
}
```

### const методи:

```cpp
class Point {
private:
    int x, y;

public:
    // const метод - обещава да не променя обекта
    int getX() const {
        return x;
    }

    // Не е const - може да променя обекта
    void setX(int newX) {
        x = newX;
    }
};

void printPoint(const Point& p) {
    std::cout << p.getX() << std::endl;  // ✅ OK - getX е const
    // p.setX(10);  // ❌ ГРЕШКА! setX не е const
}
```

---

# _КАКВО СА ENUMS?_

## 1) ИЗБРОИМИ ТИПОВЕ (ENUMERATIONS)

### Основна концепция:

**Enum** е тип, който дефинира набор от **именовани константи**. Използва се за стойности, които имат фиксиран набор от възможности.

### Синтаксис:

```cpp
enum ИмеНаEnum {
    КОНСТАНТА1,
    КОНСТАНТА2,
    КОНСТАНТА3
};
```

---

## 2) КЛАСИЧЕСКИ ENUM (C-style)

### Пример:

```cpp
enum Color {
    RED,      // 0
    GREEN,    // 1
    BLUE,     // 2
    YELLOW    // 3
};

int main() {
    Color c = RED;

    if (c == RED) {
        std::cout << "The color is red" << std::endl;
    }

    // Enum стойностите се конвертират автоматично към int
    int colorValue = RED;  // 0

    return 0;
}
```

### Задаване на конкретни стойности:

```cpp
enum HttpStatus {
    OK = 200,
    NOT_FOUND = 404,
    SERVER_ERROR = 500
};

enum Days {
    MONDAY = 1,
    TUESDAY,     // 2
    WEDNESDAY,   // 3
    THURSDAY,    // 4
    FRIDAY,      // 5
    SATURDAY,    // 6
    SUNDAY       // 7
};
```

### Проблеми с класическите enums:

```cpp
enum Color { RED, GREEN, BLUE };
enum TrafficLight { RED, YELLOW, GREEN };  // ❌ ГРЕШКА! RED е вече дефиниран

// Има конфликт на имена!
```

---

## 3) ENUM CLASS (C++11 - ПРЕПОРЪЧИТЕЛНО)

### По-сигурен вариант:

```cpp
enum class Color {
    Red,
    Green,
    Blue
};

enum class TrafficLight {
    Red,      // ✅ OK - няма конфликт
    Yellow,
    Green     // ✅ OK - няма конфликт
};

int main() {
    Color c = Color::Red;              // Трябва Color::
    TrafficLight t = TrafficLight::Red; // Трябва TrafficLight::

    // c == t;  // ❌ ГРЕШКА! Не се сравняват автоматично

    // Не се конвертира автоматично към int
    // int x = Color::Red;  // ❌ ГРЕШКА!
    int x = static_cast<int>(Color::Red);  // ✅ Explicit cast

    return 0;
}
```

**Предимства на enum class:**

- ✅ Няма конфликти на имена
- ✅ По-силна типова безопасност
- ✅ Не се конвертира автоматично към int
- ✅ По-добра енкапсулация

---

## 4) ENUM В SWITCH

```cpp
enum class Direction {
    North,
    South,
    East,
    West
};

void move(Direction dir) {
    switch (dir) {
        case Direction::North:
            std::cout << "Moving North" << std::endl;
            break;
        case Direction::South:
            std::cout << "Moving South" << std::endl;
            break;
        case Direction::East:
            std::cout << "Moving East" << std::endl;
            break;
        case Direction::West:
            std::cout << "Moving West" << std::endl;
            break;
    }
}
```

---

## 5) ENUM В СТРУКТУРИ И КЛАСОВЕ

```cpp
struct Player {
    char name[50];
    int health;

    enum class State {
        Idle,
        Running,
        Jumping,
        Attacking
    };

    State currentState;
};

int main() {
    Player p;
    strcpy(p.name, "Hero");
    p.health = 100;
    p.currentState = Player::State::Idle;

    if (p.currentState == Player::State::Attacking) {
        std::cout << p.name << " is attacking!" << std::endl;
    }

    return 0;
}
```

---

## 6) ПРАКТИЧЕСКИ ПРИМЕРИ

### Пример 1: Дни от седмицата

```cpp
enum class Weekday {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
};

bool isWeekend(Weekday day) {
    return day == Weekday::Saturday || day == Weekday::Sunday;
}

int main() {
    Weekday today = Weekday::Friday;

    if (isWeekend(today)) {
        std::cout << "It's weekend!" << std::endl;
    } else {
        std::cout << "It's a workday" << std::endl;
    }

    return 0;
}
```

### Пример 2: Статус на поръчка

```cpp
class Order {
public:
    enum class Status {
        Pending,
        Processing,
        Shipped,
        Delivered,
        Cancelled
    };

private:
    int orderNumber;
    Status status;

public:
    Order(int num) : orderNumber(num), status(Status::Pending) {}

    void updateStatus(Status newStatus) {
        status = newStatus;
    }

    const char* getStatusText() const {
        switch (status) {
            case Status::Pending: return "Pending";
            case Status::Processing: return "Processing";
            case Status::Shipped: return "Shipped";
            case Status::Delivered: return "Delivered";
            case Status::Cancelled: return "Cancelled";
            default: return "Unknown";
        }
    }
};
```

---

## 7) КОМБИНИРАНЕ: СТРУКТУРИ + ENUMS

```cpp
enum class AccountType {
    Savings,
    Checking,
    Investment
};

struct BankAccount {
    int accountNumber;
    AccountType type;
    double balance;

    void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
        }
    }

    bool withdraw(double amount) {
        if (amount > 0 && amount <= balance) {
            balance -= amount;
            return true;
        }
        return false;
    }
};

int main() {
    BankAccount acc;
    acc.accountNumber = 12345;
    acc.type = AccountType::Savings;
    acc.balance = 1000.0;

    acc.deposit(500);
    acc.withdraw(200);

    std::cout << "Balance: " << acc.balance << std::endl;

    return 0;
}
```

---

## 8) ДОБРИ ПРАКТИКИ

### ✅ За структури:

1. Използвайте структури за **прости данни без сложна логика**
2. Подреждайте членовете по размер (голям → малък) за по-малко padding
3. Използвайте `const&` при подаване към функции
4. Инициализирайте всички членове

### ✅ За класове:

1. Правете данните **private**, методите **public**
2. Винаги дефинирайте конструктор и деструктор при работа с динамична памет
3. Използвайте `const` методи за четене на данни
4. Спазвайте принципа на единичната отговорност (Single Responsibility)

### ✅ За enums:

1. Предпочитайте **enum class** вместо обикновен enum
2. Използвайте смислени имена (не RED, а Color::Red)
3. Комбинирайте с switch за обработка на всички възможни стойности
4. Дефинирайте enums в подходящия scope (глобално или в класа)

---

## 9) ЧЕСТО СРЕЩАНИ ГРЕШКИ

### ❌ Грешка 1: Забравяне на `;` след структура/клас

```cpp
struct Point {
    int x;
    int y;
}  // ❌ ГРЕШКА! Липсва ;

int main() { ... }
```

### ❌ Грешка 2: Достъп до private член

```cpp
class Point {
    int x;  // private
public:
    void setX(int val) { x = val; }
};

int main() {
    Point p;
    p.x = 10;  // ❌ ГРЕШКА! x е private
    p.setX(10);  // ✅ OK
}
```

### ❌ Грешка 3: Забравяне да освободите памет

```cpp
class MyClass {
    int* data;
public:
    MyClass(int n) {
        data = new int[n];
    }
    // ❌ ГРЕШКА! Няма деструктор - memory leak!
};

// ✅ Правилно:
class MyClass {
    int* data;
public:
    MyClass(int n) {
        data = new int[n];
    }
    ~MyClass() {
        delete[] data;  // Освобождаваме паметта
    }
};
```

---

**Успех със структурите, класовете и enums! 🚀**

_Помнете: Структурите и класовете са основа на обектно-ориентираното програмиране. Овладейте ги добре!_
